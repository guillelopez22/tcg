---
phase: 01.1-collection-ux-polish
plan: 03
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx
  - apps/web/src/app/(dashboard)/collection/wantlist-tab.tsx
  - apps/web/src/app/(dashboard)/decks/deck-wizard.tsx
autonomous: false
requirements:
  - UXP-08
  - UXP-09
  - UXP-10
  - UXP-11
  - UXP-12

must_haves:
  truths:
    - "Tradelist cards show both asking price and market price"
    - "Tradelist price editor pre-fills with market price"
    - "Wantlist cards show market price per card"
    - "Wantlist shows total want value summary at top"
    - "Remove actions on wantlist and tradelist show 5-second undo toast"
    - "Trending decks show tier badge (S/A/B/C)"
    - "Trending deck import shows card list preview before confirming"
    - "Legend picker has search bar and domain filter chips"
    - "Deck creation from legend path shows summary before Go to Editor"
    - "Non-Latin deck names show English fallback with original as subtitle"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx"
      provides: "Dual price display + market price pre-fill + undo toast"
      contains: "Mkt:"
    - path: "apps/web/src/app/(dashboard)/collection/wantlist-tab.tsx"
      provides: "Market price badges + total value + undo toast"
      contains: "totalWantValue"
    - path: "apps/web/src/app/(dashboard)/decks/deck-wizard.tsx"
      provides: "Preview step, summary step, legend search, tier badges, name translation"
      contains: "preview-suggested"
  key_links:
    - from: "apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx"
      to: "wishlist.list query"
      via: "entry.card.marketPrice for pre-fill and display"
      pattern: "marketPrice"
    - from: "apps/web/src/app/(dashboard)/decks/deck-wizard.tsx"
      to: "card.legends query"
      via: "trpc.card.legends.useQuery for legend picker"
      pattern: "card\\.legends"
    - from: "apps/web/src/app/(dashboard)/decks/deck-wizard.tsx"
      to: "deck.browse query"
      via: "tier field from browse results for badge display"
      pattern: "tier"
---

<objective>
Polish tradelist pricing UX (dual price display, market pre-fill), wantlist price display (per-card market price, total value), undo toasts on all remove actions, and deck wizard refinements (preview step, summary step, legend search, tier badges, non-Latin name handling).

Purpose: Completes the UX polish for the three remaining feature areas beyond the collection grid. Tradelist/wantlist pricing makes pricing decisions easier. Deck wizard improvements reduce friction when importing trending decks and building from legends.

Output: Modified tradelist-tab.tsx, wantlist-tab.tsx, and deck-wizard.tsx with all polish features.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-collection-ux-polish/01.1-CONTEXT.md
@.planning/phases/01.1-collection-ux-polish/01.1-RESEARCH.md
@.planning/phases/01.1-collection-ux-polish/01.1-01-SUMMARY.md

@apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx
@apps/web/src/app/(dashboard)/collection/wantlist-tab.tsx
@apps/web/src/app/(dashboard)/decks/deck-wizard.tsx

<interfaces>
<!-- After Plan 01, wishlist.list returns: entry.card.marketPrice: string | null -->
<!-- After Plan 01, card.legends endpoint returns: Array<{ id, name, cleanName, domain, imageSmall }> -->
<!-- After Plan 01, deck.browse returns: deck.tier: string | null -->

From deck-wizard.tsx (existing types):
- WizardPath = null | 'suggested' | 'legend' | 'scratch'
- WizardStep = 'choose-path' | 'pick-suggested' | 'pick-legend' | 'name-deck'
  (Plan will extend to: 'choose-path' | 'pick-suggested' | 'preview-suggested' | 'pick-legend' | 'name-deck' | 'deck-summary')
- LegendCard = { id, name, cleanName, domain, imageSmall }
- DeckWizardProps = { isOpen, onClose, onCreated }

Sonner toast undo pattern (same as collection grid):
- toast(message, { duration: 5000, action: { label: 'Undo', onClick: handler } })

Non-Latin name detection (from research):
- /[\u4E00-\u9FFF\u3400-\u4DBF\uF900-\uFAFF]/.test(text) detects CJK
- Fallback: legend name + domain if available
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tradelist and wantlist pricing UX + undo toasts</name>
  <files>
    apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx
    apps/web/src/app/(dashboard)/collection/wantlist-tab.tsx
  </files>
  <action>
    **tradelist-tab.tsx:**

    1. **Dual price display:** For each tradelist card, show both asking price and market price below the card image. Format: `Ask: $5.00 | Mkt: $4.50`. Use `entry.card.marketPrice` (available after Plan 01). When no asking price is set, show only `Mkt: $4.50`. When no market price exists, show only the asking price or nothing. Use `text-rift-400` for asking price, `text-zinc-400` for market price, `tabular-nums` for alignment.

    2. **Price editor pre-fill:** When user opens the inline price editor, pre-fill the price input with `entry.card.marketPrice ?? ''`. Currently the editor likely opens blank or with the existing askingPrice. Change default value to: if `entry.askingPrice` exists use it, else use `entry.card.marketPrice ?? ''`.

    3. **Action buttons hover/visible:** The action buttons (price edit, remove) should use `opacity-100 md:opacity-0 md:group-hover:opacity-100 transition-opacity` on desktop (hover reveal) and always visible on mobile. Ensure the card wrapper has `group` class.

    4. **Undo toast on remove:** When removing a tradelist entry, show 5-second sonner undo toast. Capture `{ cardId, type: 'trade' }` before removing. Undo handler calls `wishlist.toggle` to re-add (toggle adds when not present). Pattern:
       ```
       const toastId = toast('Removed from tradelist', {
         duration: 5000,
         action: { label: 'Undo', onClick: () => {
           toggleMutation.mutate({ cardId: entry.cardId, type: 'trade' });
           toast.dismiss(toastId);
         }},
       });
       ```

    **wantlist-tab.tsx:**

    1. **Market price badge:** Show market price on each wantlist card: `$X.XX` badge below the card image. Use `entry.card.marketPrice`. Show only when non-null. Use `text-zinc-400 text-[10px] tabular-nums`.

    2. **Total want value summary:** At the top of the wantlist section, compute and display total want value:
       ```
       const totalWantValue = useMemo(() => {
         if (!data?.pages) return 0;
         return data.pages.flatMap(p => p.items).reduce((sum, entry) => {
           return sum + parseFloat(entry.card.marketPrice ?? '0');
         }, 0);
       }, [data?.pages]);
       ```
       Display: `<div className="text-zinc-400 text-sm mb-2">Total want value: $${totalWantValue.toFixed(2)}</div>` at the top of the list. Import `useMemo` from React.

    3. **Undo toast on remove:** Same pattern as tradelist — capture `{ cardId, type: 'want' }`, show 5-second undo toast with "Undo" action that re-toggles the entry.

    **Both files:** Import `toast` from `sonner` if not already imported. Use `useTranslations()` for any new UI strings (add translation keys for "Copy removed", "Removed from tradelist", "Removed from wantlist", "Undo", "Total want value").
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - Tradelist shows dual price (Ask + Mkt) per card
    - Price editor pre-fills with market price when no asking price set
    - Action buttons hover-reveal on desktop, always visible on mobile
    - Tradelist remove shows 5-second undo toast
    - Wantlist shows market price per card
    - Wantlist shows total want value at top
    - Wantlist remove shows 5-second undo toast
    - All new strings use useTranslations()
  </done>
</task>

<task type="auto">
  <name>Task 2: Deck wizard refinements (preview, summary, legend search, tier badges, name translation)</name>
  <files>apps/web/src/app/(dashboard)/decks/deck-wizard.tsx</files>
  <action>
    Modify deck-wizard.tsx to add five improvements. Read the full file first to understand the current wizard flow.

    **1. Extended WizardStep type:**
    Change `type WizardStep = 'choose-path' | 'pick-suggested' | 'pick-legend' | 'name-deck'` to:
    `type WizardStep = 'choose-path' | 'pick-suggested' | 'preview-suggested' | 'pick-legend' | 'name-deck' | 'deck-summary'`.

    **2. Trending deck tier badges:**
    In the `pick-suggested` step where trending/public decks are listed, show a tier badge next to each deck name. The `deck.browse` query now returns `tier: string | null` (from Plan 01). Render a colored badge: S=gold, A=emerald, B=blue, C=zinc. Only show badge when tier is non-null. Example: `<span className="text-xs font-bold px-1.5 py-0.5 rounded bg-yellow-500/20 text-yellow-400">S</span>`.

    **3. Non-Latin deck name display:**
    Add a helper function at the top of the file:
    ```
    function isNonLatinScript(text: string): boolean {
      return /[\u4E00-\u9FFF\u3400-\u4DBF\uF900-\uFAFF]/.test(text);
    }
    function getDisplayName(rawName: string, domain?: string | null): { primary: string; subtitle: string | null } {
      const cleaned = rawName.replace(/^\[RD\]\s*/, '');
      if (isNonLatinScript(cleaned)) {
        return { primary: domain ? `${domain} Deck` : 'Imported Deck', subtitle: cleaned };
      }
      return { primary: cleaned, subtitle: null };
    }
    ```
    Use `getDisplayName()` when rendering deck names in the trending list. Show `subtitle` in a smaller `text-zinc-500` span below the primary name.

    **4. Trending deck preview step (`preview-suggested`):**
    When user selects a trending deck in `pick-suggested`, instead of immediately importing:
    - Set `selectedDeckId` state, transition to `'preview-suggested'` step.
    - In the preview step, fetch the full deck via `trpc.deck.getById.useQuery({ id: selectedDeckId })`.
    - Group the deck's cards by `cardType` (Legend, Creature, Rune, etc.) and display counts + card names per group.
    - Show a "Confirm Import" button that fires the existing import logic.
    - Show a "Back" button to return to `pick-suggested`.

    **5. Legend picker with search and domain filters:**
    Replace the current chained `card.list` pagination queries with a single `trpc.card.legends.useQuery()` call (from Plan 01 endpoint).
    - Add state: `const [legendSearch, setLegendSearch] = useState('')` and `const [legendDomainFilter, setLegendDomainFilter] = useState<string | null>(null)`.
    - Filter the legends array client-side by search (case-insensitive `cleanName.includes`) and domain filter.
    - Render domain filter chips at the top: one chip per unique domain from the legends data. Use the existing `DOMAIN_COLORS` map for styling. Clicking a chip toggles the filter; clicking the active chip clears it.
    - Render a search input above the legend grid: `<input type="text" placeholder="Search legends..." className="lg-input w-full mb-3" value={legendSearch} onChange={...} />`.

    **6. Deck summary step (`deck-summary`):**
    After creating a deck via the legend path (after `handleCreateFromLegend` succeeds):
    - Instead of immediately calling `router.push`, transition to `'deck-summary'` step.
    - Add state: `const [deckSummary, setDeckSummary] = useState<{ deckId: string; cardCount: number; domainBreakdown: Record<string, number> } | null>(null)`.
    - Compute domain breakdown from the starter cards used to create the deck.
    - Display: "Deck Created!" heading, total card count, domain breakdown as colored badges, and a "Go to Editor" button that calls `router.push(/decks/${deckSummary.deckId})`.
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - WizardStep type includes 'preview-suggested' and 'deck-summary'
    - Trending decks show tier badge (S/A/B/C with color coding)
    - Non-Latin deck names show English fallback with original as subtitle
    - Preview step shows grouped card list before confirming import
    - Legend picker uses card.legends endpoint (single query, not 6 chained)
    - Legend picker has search bar and domain filter chips
    - Summary step shows card count and domain breakdown after legend-path creation
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UX polish for collection grid, tradelist/wantlist pricing, and deck wizard across Plans 02 and 03</what-built>
  <how-to-verify>
    1. Start dev servers: `pnpm dev` (or however the project starts)
    2. Log in with test@test.com / PassWord123
    3. **Collection grid:**
       - Tap + on a card — should add instantly (no flash)
       - Tap + and hold for 500ms — should open variant/condition picker
       - Select a variant and condition, tap Add — new copy added
       - Tap - on a card with 1 copy — should remove and show undo toast for 5s
       - Tap "Undo" on the toast — copy should reappear
       - Tap - on a card with 2+ copies — should show copy picker
       - Check filter bar area for "Total: $X.XX" badge
       - On desktop: verify steppers appear only on hover
    4. **Tradelist:**
       - Navigate to Collection > Tradelist tab
       - Each card should show "Ask: $X | Mkt: $X"
       - Open price editor — should pre-fill with market price
       - Remove a card — undo toast should appear
    5. **Wantlist:**
       - Navigate to Collection > Wantlist tab
       - Each card should show market price badge
       - "Total want value: $X" should appear at top
       - Remove a card — undo toast should appear
    6. **Deck wizard:**
       - Open deck wizard > Suggested path
       - Trending decks should show tier badges (S/A/B/C)
       - Select a trending deck — preview step should show card list grouped by type
       - Click "Confirm Import" to import
       - Open wizard > Build Around Legend
       - Search bar and domain filter chips should appear
       - Type a legend name — list should filter
       - Click a domain chip — list should filter by domain
       - Select a legend, name the deck, create — summary step should show card count and domain breakdown
       - Click "Go to Editor" to navigate
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/web/tsconfig.json` passes
- Grep for `Mkt:` in tradelist-tab.tsx confirms dual price display
- Grep for `totalWantValue` in wantlist-tab.tsx confirms total value computation
- Grep for `preview-suggested` in deck-wizard.tsx confirms preview step exists
- Grep for `card.legends` in deck-wizard.tsx confirms legends endpoint usage
- Grep for `isNonLatinScript` in deck-wizard.tsx confirms name translation
- Grep for `tier` in deck-wizard.tsx confirms tier badge rendering
</verification>

<success_criteria>
All tradelist, wantlist, and deck wizard UX improvements are implemented:
1. Tradelist: dual price, pre-fill, hover actions, undo toast
2. Wantlist: per-card price, total value, undo toast
3. Deck wizard: tier badges, name translation, preview step, legend search/filter, summary step
4. Human verification confirms all interactions work as specified
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-collection-ux-polish/01.1-03-SUMMARY.md`
</output>
