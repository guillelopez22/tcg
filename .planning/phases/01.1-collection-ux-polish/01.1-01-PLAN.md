---
phase: 01.1-collection-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/decks.ts
  - apps/api/src/modules/wishlist/wishlist.service.ts
  - apps/api/src/modules/collection/collection.service.ts
  - apps/api/src/modules/card/card.service.ts
  - apps/api/src/modules/card/card.router.ts
  - tools/seed/src/sync-riftdecks.ts
autonomous: true
requirements:
  - UXP-01
  - UXP-02
  - UXP-03
  - UXP-04

must_haves:
  truths:
    - "Wishlist list() returns marketPrice for each card"
    - "Collection list() returns marketPrice for each card"
    - "card.legends endpoint returns only Legend-type cards with id, name, cleanName, domain, imageSmall"
    - "Decks table has a tier column and sync-riftdecks populates it"
    - "deck.browse returns tier field for each deck"
  artifacts:
    - path: "apps/api/src/modules/wishlist/wishlist.service.ts"
      provides: "Wishlist list with card_prices LEFT JOIN"
      contains: "cardPrices"
    - path: "apps/api/src/modules/collection/collection.service.ts"
      provides: "Collection list with card_prices LEFT JOIN"
      contains: "cardPrices"
    - path: "apps/api/src/modules/card/card.service.ts"
      provides: "getLegends() method"
      contains: "getLegends"
    - path: "apps/api/src/modules/card/card.router.ts"
      provides: "legends tRPC query"
      contains: "legends"
    - path: "packages/db/src/schema/decks.ts"
      provides: "tier column on decks table"
      contains: "tier"
  key_links:
    - from: "apps/api/src/modules/wishlist/wishlist.service.ts"
      to: "packages/db/src/schema/card-prices.ts"
      via: "LEFT JOIN in list()"
      pattern: "leftJoin.*cardPrices"
    - from: "apps/api/src/modules/card/card.router.ts"
      to: "apps/api/src/modules/card/card.service.ts"
      via: "legends query calls getLegends()"
      pattern: "getLegends"
---

<objective>
Add backend support for Phase 01.1 UX polish features: market price data in wishlist/collection list queries, a dedicated legends endpoint for the deck wizard, and a tier column on decks for trending deck badges.

Purpose: All four frontend plans (collection grid total badge, wantlist/tradelist price display, deck wizard legend picker, trending deck tier badges) depend on these backend changes being in place first.

Output: Modified service files with price joins, new card.legends tRPC query, tier column on decks schema + sync script update.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-collection-ux-polish/01.1-CONTEXT.md
@.planning/phases/01.1-collection-ux-polish/01.1-RESEARCH.md

@apps/api/src/modules/wishlist/wishlist.service.ts
@apps/api/src/modules/collection/collection.service.ts
@apps/api/src/modules/card/card.service.ts
@apps/api/src/modules/card/card.router.ts
@packages/db/src/schema/decks.ts
@apps/api/src/modules/deck/deck.service.ts
@tools/seed/src/sync-riftdecks.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/db/src/schema/card-prices.ts:
- cardPrices table with: id, cardId, tcgplayerProductId, lowPrice, midPrice, highPrice, marketPrice, directLowPrice, foilLowPrice, foilMidPrice, foilHighPrice, foilMarketPrice, foilDirectLowPrice, updatedAt, createdAt

From apps/api/src/modules/wishlist/wishlist.service.ts:
- WishlistCard interface: { id, name, imageSmall, imageLarge, rarity, setId, setSlug, setName }
- WishlistEntryWithCard = Wishlist & { card: WishlistCard }
- list() returns PaginatedResult<WishlistEntryWithCard>

From apps/api/src/modules/collection/collection.service.ts:
- CollectionEntryWithCard = Collection & { card: Card & { set: Set } }
- list() returns PaginatedResult<CollectionEntryWithCard>

From apps/api/src/modules/deck/deck.service.ts:
- DeckWithCreator = Deck & { user: { username, displayName } }
- browse() returns PaginatedResult<DeckWithCreator>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tier column to decks + price joins in wishlist and collection list queries</name>
  <files>
    packages/db/src/schema/decks.ts
    apps/api/src/modules/wishlist/wishlist.service.ts
    apps/api/src/modules/collection/collection.service.ts
    apps/api/src/modules/deck/deck.service.ts
    tools/seed/src/sync-riftdecks.ts
  </files>
  <action>
    1. **packages/db/src/schema/decks.ts** — Add a nullable `tier` column: `tier: varchar('tier', { length: 2 })` (stores 'S', 'A', 'B', 'C', or null for user-created decks). Add it after `domain`. This is schema-only; the migration SQL will be a simple `ALTER TABLE decks ADD COLUMN tier varchar(2)` run via a migration script (create `tools/seed/src/migrate-01.1-01.cjs` using the same pattern as `migrate-01-01.cjs` from Phase 1 — direct SQL via `pg` client reading `DATABASE_URL`).

    2. **tools/seed/src/sync-riftdecks.ts** — The scraper fetches tier data from `scrapeTierListChampions()` but discards it after filtering. Pass the champion's tier through to the upsert:
       - In the deck URL collection loop, attach `tier` to each deck link object: `deckLinks.push(...championDecks.map(d => ({ ...d, tier: champion.tier })))`.
       - In the upsert section (around line 265-290), add `tier` to both the `.set({})` (update) and `.values({})` (insert) calls using the tier from the deck link.
       - Update the `deckLinks` type from `Array<{ url: string; name: string }>` to `Array<{ url: string; name: string; tier: string }>`.

    3. **apps/api/src/modules/deck/deck.service.ts** — In `browse()` method:
       - Add `tier: decks.tier` to the select fields (around line 286).
       - Add `tier: r.tier` to the row mapping (around line 312).
       - Update `DeckWithCreator` type to include `tier: string | null`.

    4. **apps/api/src/modules/wishlist/wishlist.service.ts** — In `list()`:
       - Import `cardPrices` from `@la-grieta/db`.
       - Add `leftJoin(cardPrices, eq(cards.id, cardPrices.cardId))` after the existing `innerJoin(sets, ...)`.
       - Add `card_marketPrice: cardPrices.marketPrice` to the select fields.
       - Add `marketPrice: r.card_marketPrice` to the `card` object in the row mapping.
       - Update `WishlistCard` interface to include `marketPrice: string | null`.

    5. **apps/api/src/modules/collection/collection.service.ts** — In `list()`:
       - The service already imports `cardPrices`. Add `leftJoin(cardPrices, eq(cards.id, cardPrices.cardId))` after the existing `innerJoin(sets, ...)`.
       - Add `card_marketPrice: cardPrices.marketPrice, card_foilMarketPrice: cardPrices.foilMarketPrice` to the select fields.
       - Add `marketPrice: r.card_marketPrice, foilMarketPrice: r.card_foilMarketPrice` to the `card` object in the row mapping.
       - Update `CollectionEntryWithCard` type — the `card` property should extend with `{ marketPrice: string | null; foilMarketPrice: string | null }`.

    **Important:** Use LEFT JOIN (not INNER JOIN) for card_prices in both services. Not all cards have price data. Null-coalesce in the row mapping if needed.
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - decks schema has tier varchar(2) column
    - Migration script exists and can add the column
    - sync-riftdecks passes tier through to DB upsert
    - deck.browse() returns tier field
    - wishlist.list() returns marketPrice per card via LEFT JOIN
    - collection.list() returns marketPrice and foilMarketPrice per card via LEFT JOIN
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add card.legends tRPC endpoint</name>
  <files>
    apps/api/src/modules/card/card.service.ts
    apps/api/src/modules/card/card.router.ts
  </files>
  <action>
    1. **apps/api/src/modules/card/card.service.ts** — Add `getLegends()` method:
       ```
       async getLegends(): Promise<Array<{ id: string; name: string; cleanName: string; domain: string; imageSmall: string | null }>> {
         const cached = await this.redis.get('cache:card_legends');
         if (cached) return JSON.parse(cached);

         const rows = await this.db
           .select({
             id: cards.id,
             name: cards.name,
             cleanName: cards.cleanName,
             domain: cards.domain,
             imageSmall: cards.imageSmall,
           })
           .from(cards)
           .where(and(eq(cards.cardType, 'Legend'), eq(cards.isProduct, false)))
           .orderBy(cards.name);

         await this.redis.setex('cache:card_legends', 3600, JSON.stringify(rows));
         return rows;
       }
       ```
       Uses Redis cache (1 hour TTL) since legends change only on card sync. Import `and` if not already imported.

    2. **apps/api/src/modules/card/card.router.ts** — Add a public `legends` query (no auth required, same as `list`):
       ```
       legends: pub
         .query(async () => {
           return this.cardService.getLegends();
         }),
       ```
       Place it after the existing `list` query. Use the same `pub` procedure builder pattern used by other public queries in this router.
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/api/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    - card.legends tRPC query exists and is public
    - Returns only Legend-type, non-product cards with id, name, cleanName, domain, imageSmall
    - Results cached in Redis for 1 hour
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/api/tsconfig.json` passes
- `npx tsc --noEmit -p packages/db/tsconfig.json` passes
- Migration script exists at `tools/seed/src/migrate-01.1-01.cjs`
- Grep confirms: `cardPrices` imported and used in wishlist.service.ts
- Grep confirms: `getLegends` method exists in card.service.ts
- Grep confirms: `tier` field in decks schema
</verification>

<success_criteria>
All backend queries needed by the frontend plans are in place:
1. wishlist.list returns marketPrice
2. collection.list returns marketPrice + foilMarketPrice
3. card.legends returns Legend cards efficiently
4. deck.browse returns tier
5. All TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-collection-ux-polish/01.1-01-SUMMARY.md`
</output>
