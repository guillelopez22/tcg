---
phase: 02-deck-builder-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx
  - apps/web/src/app/(dashboard)/decks/[id]/deck-card-editor.tsx
  - apps/web/src/app/(dashboard)/decks/[id]/hand-simulator.tsx
  - apps/web/src/app/(dashboard)/decks/import-deck-modal.tsx
  - apps/web/src/app/(dashboard)/decks/import-preview.tsx
  - apps/web/src/app/(dashboard)/decks/deck-list.tsx
autonomous: true
requirements: [DECK-03, DECK-05, DECK-06, DECK-07]

must_haves:
  truths:
    - "User can click 'Draw Sample Hand' on any deck and see 4 random cards from the main deck displayed horizontally"
    - "User can click 'Mulligan' to set aside up to 2 cards and draw replacements"
    - "User can click 'Share' on a public deck and get an LG-XXXXXX code copied to clipboard with toast confirmation"
    - "User can open an import modal on /decks, paste text or enter a share code or URL, see a preview of resolved cards with unmatched warnings, and import to their decks"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/decks/[id]/hand-simulator.tsx"
      provides: "Hand simulator UI component"
      min_lines: 50
    - path: "apps/web/src/app/(dashboard)/decks/import-deck-modal.tsx"
      provides: "3-tab import modal (Share Code | Text Paste | URL)"
      min_lines: 80
    - path: "apps/web/src/app/(dashboard)/decks/import-preview.tsx"
      provides: "Read-only deck preview with resolved/unmatched cards and Import button"
      min_lines: 40
  key_links:
    - from: "apps/web/src/app/(dashboard)/decks/[id]/hand-simulator.tsx"
      to: "@la-grieta/shared drawHand"
      via: "direct import, client-side only"
      pattern: "drawHand"
    - from: "apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx"
      to: "trpc.deck.generateShareCode"
      via: "mutation call on Share button click"
      pattern: "generateShareCode"
    - from: "apps/web/src/app/(dashboard)/decks/import-deck-modal.tsx"
      to: "trpc.deck.resolveShareCode|importFromText|importFromUrl"
      via: "resolveShareCode via .fetch() (query), importFromText/importFromUrl via .mutate() (mutations)"
      pattern: "resolveShareCode|importFromText|importFromUrl"
---

<objective>
Build the UI for hand simulation, share code generation, and deck import (share code + text paste + URL). These features appear on the deck detail page (share button, hand simulator) and the /decks page (import modal).

Purpose: Delivers the user-facing features for DECK-03 (hand simulation), DECK-05 (share), DECK-06 (import from code), and DECK-07 (import from text/URL).
Output: Hand simulator component, share button on detail + editor, 3-tab import modal with preview step.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-deck-builder-enhancements/02-CONTEXT.md
@.planning/phases/02-deck-builder-enhancements/02-RESEARCH.md
@.planning/phases/02-deck-builder-enhancements/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (shared utilities + API endpoints) -->

From packages/shared/src/utils/draw-hand.ts:
```typescript
export interface HandCard {
  cardId: string;
  name: string;
  imageSmall: string | null;
}
export function drawHand(
  deckCards: Array<{ cardId: string; quantity: number; name: string; imageSmall: string | null }>,
  handSize?: number, // defaults to 4
): HandCard[];
```

From packages/shared/src/schemas/deck.schema.ts (new schemas):
```typescript
export const shareCodeGenerateSchema = z.object({ deckId: z.string().uuid() });
export const shareCodeResolveSchema = z.object({ code: z.string().min(3).max(12) });
export const deckImportTextSchema = z.object({ text: z.string().min(1).max(10000), name: z.string().min(1).max(100).optional() });
export const deckImportUrlSchema = z.object({ url: z.string().url().max(500), name: z.string().min(1).max(100).optional() });
```

New tRPC endpoints (from Plan 01 Task 2):
```typescript
// deck.generateShareCode: mutation, returns string (the code)
// deck.resolveShareCode: QUERY (not mutation), returns DeckWithCards — call with .fetch() or useQuery(), NOT .mutate()
// deck.importFromText: mutation, returns { resolved: DeckCardEntry[]; unmatched: string[]; deckName: string }
// deck.importFromUrl: mutation, returns { resolved: DeckCardEntry[]; unmatched: string[]; deckName: string }
```

Existing deck-detail.tsx structure:
```typescript
// Tabbed layout: 'cards' | 'analytics'
// DeckStatusBadge, BuildabilitySection sub-components
// Uses trpc.deck.getById query
// Uses useAuth() for ownership check
// DeckCardEditor imported for card editing tab
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hand simulator component + integration into deck detail</name>
  <files>
    apps/web/src/app/(dashboard)/decks/[id]/hand-simulator.tsx
    apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx
  </files>
  <action>
    1. Create `hand-simulator.tsx` as a 'use client' component:
       - Props: `cards: Array<{ cardId: string; quantity: number; name: string; imageSmall: string | null; zone: string }>` (the full deck cards array from getById)
       - Filter to only main-zone cards: `cards.filter(c => c.zone === 'main')`
       - State: `hand: HandCard[]` (initially empty), `mulliganMode: boolean`, `selectedForMulligan: Set<number>` (indices)
       - "Draw Sample Hand" button: calls `drawHand(mainCards, 4)`, sets hand state, resets mulligan state
       - Display hand as horizontal row of card images (small size, ~80px wide), each card showing name below image
       - If card has no imageSmall, show placeholder with card name
       - "Mulligan" button (only visible when hand is drawn and mulliganMode is false): enters mulligan mode
       - In mulligan mode: cards become selectable (click to toggle, max 2), "Confirm Mulligan" button
       - Confirm mulligan: remove selected cards from hand, draw replacements from remaining pool (re-shuffle pool minus current hand, take N where N = number selected), exit mulligan mode
       - "Draw Again" button: same as initial draw, full reshuffle
       - Use `lg-btn-secondary` for buttons, `lg-card` for the container section
       - UI label: "Sample Hand" as section title (not "Hand Simulator" -- per policy, frame as preview tool)
       - No probability stats, no win-rate, no animation beyond simple opacity transition

    2. Integrate into `deck-detail.tsx`:
       - Import HandSimulator component
       - Add a new section below the existing tabs (below analytics), visible when deck has main-zone cards
       - Only show when deck has cards (not on empty decks)
       - Pass the deck's cards array (from getById data) to HandSimulator

    NOTE: drawHand is a pure client-side function imported from @la-grieta/shared. No server round-trip. This is a visual consistency preview tool per Riot Digital Tools Policy.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit src/app/\(dashboard\)/decks/\[id\]/hand-simulator.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - HandSimulator component renders 4 card images in a horizontal row when "Draw Sample Hand" is clicked
    - Mulligan mode allows selecting up to 2 cards and replacing them
    - "Draw Again" reshuffles and draws a new hand
    - Component only uses main-zone cards (not runes, not champion)
    - No server calls, no probability stats
    - Section visible on deck detail page below analytics
  </done>
</task>

<task type="auto">
  <name>Task 2: Share button on deck detail and editor toolbar</name>
  <files>
    apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx
    apps/web/src/app/(dashboard)/decks/[id]/deck-card-editor.tsx
  </files>
  <action>
    1. Add share button to `deck-detail.tsx`:
       - Only visible when the deck isPublic === true AND user is the deck owner
       - Button styled with `lg-btn-secondary` with a share icon (simple SVG arrow-up-from-square or link icon)
       - On click: call `trpc.deck.generateShareCode.mutate({ deckId })` mutation
       - On success: copy the returned code to clipboard via `navigator.clipboard.writeText(code)`, show `toast.success('Share code copied: ' + code)`
       - On error: show `toast.error(error.message)` (handles "Only public decks" error from server)
       - Place the button in the deck header area next to the edit/delete buttons
       - If deck is not public, show a disabled "Share" button with tooltip "Make deck public to share" (or just hide it entirely -- simpler)

    2. Add share button to `deck-card-editor.tsx` toolbar:
       - Same logic as above: only when isPublic and user is owner
       - Add to the toolbar row (where save/cancel buttons are)
       - Same mutation + clipboard + toast pattern
       - Requires the deckId and isPublic props -- check what props DeckCardEditor already receives and add if needed

    3. Use the existing sonner toast pattern (already used throughout the app). Use navigator.clipboard.writeText (standard Web API, already used in the codebase pattern from 02-RESEARCH.md).

    NOTE: Per user decision, private decks cannot generate share codes. The server enforces this, but the UI should also hide/disable the button for private decks to avoid unnecessary error toasts.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit src/app/\(dashboard\)/decks/\[id\]/deck-detail.tsx src/app/\(dashboard\)/decks/\[id\]/deck-card-editor.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - Share button visible on deck detail page header for owned public decks
    - Share button visible on deck card editor toolbar for owned public decks
    - Clicking share generates code, copies to clipboard, shows toast with the code
    - Private decks do not show the share button
    - Error handling shows server error message via toast
  </done>
</task>

<task type="auto">
  <name>Task 3: Import deck modal with 3 tabs and preview step</name>
  <files>
    apps/web/src/app/(dashboard)/decks/import-deck-modal.tsx
    apps/web/src/app/(dashboard)/decks/import-preview.tsx
    apps/web/src/app/(dashboard)/decks/deck-list.tsx
  </files>
  <action>
    1. Create `import-preview.tsx` as a 'use client' component:
       - Props: `resolved: Array<{ cardId, quantity, zone, name?, imageSmall? }>`, `unmatched: string[]`, `deckName: string`, `onImport: () => void`, `onBack: () => void`, `isImporting: boolean`
       - Show deck name as heading
       - Show resolved cards grouped by zone (champion, main, rune) as a list with card name + quantity + small image
       - If unmatched.length > 0: show warning banner "N cards not found: [comma-separated names]. Import the M that matched?"
       - "Import to My Decks" button (`lg-btn-primary`): calls onImport
       - "Back" button (`lg-btn-secondary`): calls onBack
       - Use `lg-card` for styling

    2. Create `import-deck-modal.tsx` as a 'use client' component:
       - Props: `isOpen: boolean`, `onClose: () => void`
       - 3-tab layout: Share Code | Text Paste | URL
       - Tab styling: use the existing tabbed pattern from deck-list.tsx (button row with active/inactive states)

       **Share Code tab:**
       - Text input for share code (placeholder "LG-a3Xk9m")
       - "Look Up" button: calls `trpc.deck.resolveShareCode.fetch({ code })` — NOTE: resolveShareCode is a tRPC QUERY, so use `.fetch()` (or `useQuery` with manual trigger via `enabled: false` + `refetch()`), NOT `.mutate()`
       - On success: switch to preview view showing the resolved deck (use ImportPreview but adapted for share code -- show full deck with cards, analytics via computeAnalytics, buildability if user is logged in)
       - The "Import to My Decks" button on preview calls `trpc.deck.create.mutate()` with the resolved deck data (name: "Copy of [original name]", cards: deck's card entries)
       - On error: show toast with error message

       **Text Paste tab:**
       - Textarea for pasting deck list text (placeholder "Paste your deck list here...\n\n3x Card Name\n2x Other Card\n...")
       - Optional deck name input
       - "Parse" button: calls `trpc.deck.importFromText.mutate({ text, name })`
       - On success: switch to ImportPreview with resolved/unmatched data
       - Import button on preview: calls `trpc.deck.create.mutate()` with resolved entries as cards array
       - If no resolved entries: show error "No cards could be matched"

       **URL tab:**
       - URL input (placeholder "https://riftbound.gg/deck/...")
       - Optional deck name input
       - "Fetch & Parse" button: calls `trpc.deck.importFromUrl.mutate({ url, name })`
       - Same preview flow as Text Paste
       - Loading state while fetching (show spinner)

       After successful import (deck.create returns), show success toast "Deck imported!" and call onClose, then redirect to the new deck page via router.push(`/decks/${newDeck.id}`)

    3. Add "Import Deck" button to `deck-list.tsx`:
       - Place next to the existing "New Deck" button in the My Decks tab header
       - Opens ImportDeckModal
       - Button styled with `lg-btn-secondary` with a download/import icon
       - Import state: `showImportModal: boolean`

    NOTE: Per user decision, importing always creates a new deck (no merge). Preview is required before import (no direct import). The server does NOT create the deck during importFromText/importFromUrl -- it only resolves card names. The client calls deck.create to actually create the deck after user confirms the preview.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit src/app/\(dashboard\)/decks/import-deck-modal.tsx src/app/\(dashboard\)/decks/import-preview.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    - Import modal opens from /decks page with 3 tabs
    - Share Code tab: looks up code via .fetch() (query), shows preview, imports as new deck
    - Text Paste tab: parses text, shows preview with unmatched warnings, imports
    - URL tab: fetches URL server-side, parses, shows preview, imports
    - Preview always shown before import (per user decision)
    - Unmatched card names displayed in warning banner
    - After import, user redirected to the new deck page
    - "Import Deck" button visible next to "New Deck" in deck list
  </done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes without errors
- Hand simulator draws 4 cards, supports mulligan, uses only main-deck pool
- Share button generates code and copies to clipboard for public decks
- Import modal supports all 3 input methods with preview step
- All new components use lg-* design system classes
</verification>

<success_criteria>
- Hand simulator section visible on deck detail page with Draw/Mulligan/Draw Again functionality
- Share button on deck detail header and editor toolbar for owned public decks
- Import modal accessible from /decks with Share Code, Text Paste, and URL tabs
- Preview step shows resolved cards + unmatched warnings before creating deck
- All interactions use sonner toasts for feedback
</success_criteria>

<output>
After completion, create `.planning/phases/02-deck-builder-enhancements/02-02-SUMMARY.md`
</output>
