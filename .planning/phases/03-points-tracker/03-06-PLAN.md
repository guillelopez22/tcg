---
phase: 03-points-tracker
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/dashboard-nav.tsx
  - apps/api/src/modules/deck/deck.service.ts
  - apps/api/src/modules/news/news.service.ts
  - apps/api/src/modules/news/news.router.ts
autonomous: true
gap_closure: true
requirements:
  - PTS-05
  - DECK-05

must_haves:
  truths:
    - "Match tab shows visible crossed-sword icon in dashboard nav"
    - "Importing a valid tournament deck does not inflate main zone count"
    - "News feed displays articles on dashboard home"
  artifacts:
    - path: "apps/web/src/components/dashboard-nav.tsx"
      provides: "Fixed IconMatch SVG with explicit stroke on lines"
      contains: "stroke="
    - path: "apps/api/src/modules/deck/deck.service.ts"
      provides: "Zone-corrected importFromText"
      contains: "getZoneForCardType"
    - path: "apps/api/src/modules/news/news.router.ts"
      provides: "Manual sync trigger endpoint"
      contains: "triggerSync"
    - path: "apps/api/src/modules/news/news.service.ts"
      provides: "Startup sync and null-safe ordering"
  key_links:
    - from: "apps/api/src/modules/deck/deck.service.ts"
      to: "@la-grieta/shared getZoneForCardType"
      via: "zone correction in importFromText"
      pattern: "getZoneForCardType"
    - from: "apps/api/src/modules/news/news.router.ts"
      to: "apps/api/src/modules/news/news.service.ts"
      via: "triggerSync calls syncCron"
      pattern: "syncCron"
---

<objective>
Fix three independent UAT issues: match tab icon not rendering, deck import miscounting main zone, and empty news feed.

Purpose: Close UAT gaps from tests 2, 14, and 15 without touching the match setup flow.
Output: Three targeted fixes that resolve cosmetic, data, and validation issues.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-points-tracker/03-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix IconMatch SVG and deck import zone correction</name>
  <files>apps/web/src/components/dashboard-nav.tsx, apps/api/src/modules/deck/deck.service.ts</files>
  <action>
1. In `apps/web/src/components/dashboard-nav.tsx`, find the `IconMatch` function (line ~151). The two `<line>` elements (crossed swords) are missing explicit stroke attributes. The parent `<svg>` has `stroke="currentColor"` but some renderers don't inherit to `<line>`. Add `stroke="currentColor"` and `strokeWidth="1.5"` to BOTH `<line>` elements. Also add them to the `<circle>` element for consistency.

2. In `apps/api/src/modules/deck/deck.service.ts`, find `importFromText()` method (line ~880). After card name resolution in the for-loop (after the `if (matched)` block, line ~894-895), the resolved entry uses `entry.zone` directly from the parser. This is wrong because Champion Units, Legends, Battlefields, and Runes get assigned to `main` zone by the parser.

   Apply the same zone correction pattern used in `create()` (line ~262-270) and `setCards()` (line ~426-432):
   - After the for-loop resolves all cards, build a cardTypeMap by querying the DB for all resolved card IDs to get their cardType
   - Map over `resolved` array and apply `getZoneForCardType(cardTypeMap.get(c.cardId) ?? null)` to derive the correct zone
   - Use the same pattern as setCards: `derivedZone !== 'main' ? derivedZone : c.zone` so explicitly-zoned main cards stay in main

   `getZoneForCardType` is already imported from `@la-grieta/shared`. `buildCardTypeMap` is a private method already on the service class — use `this.buildCardTypeMap()`.
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx vitest run apps/api/__tests__/deck.service.spec.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>IconMatch SVG lines have explicit stroke attributes. importFromText applies getZoneForCardType to resolved entries, so Champion Units go to champion zone instead of inflating main count.</done>
</task>

<task type="auto">
  <name>Task 2: Add news manual sync endpoint and startup trigger</name>
  <files>apps/api/src/modules/news/news.service.ts, apps/api/src/modules/news/news.router.ts</files>
  <action>
1. In `apps/api/src/modules/news/news.service.ts`:
   - Add an `onModuleInit()` method (implement `OnModuleInit` interface from `@nestjs/common`) that calls `this.syncCron()` on startup. This ensures dev environments get news data immediately without waiting 4 hours.
   - Update the `getLatest()` query ordering: use `desc(coalesce(newsArticles.publishedAt, newsArticles.scrapedAt))` or use SQL template: `desc(sql\`COALESCE(${newsArticles.publishedAt}, ${newsArticles.scrapedAt})\`)` to handle NULL publishedAt values. Import `sql` from `drizzle-orm` if not already imported.

2. In `apps/api/src/modules/news/news.router.ts`:
   - Add a `triggerSync` mutation (use `this.trpc.protectedProcedure` so only authenticated users can trigger it):
     ```
     triggerSync: this.trpc.protectedProcedure
       .mutation(async () => {
         await this.newsService.syncCron();
         return { success: true };
       }),
     ```
   This gives a manual way to populate news without restarting the server.
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && grep -n "onModuleInit\|triggerSync\|COALESCE\|coalesce" apps/api/src/modules/news/news.service.ts apps/api/src/modules/news/news.router.ts</automated>
  </verify>
  <done>News service syncs on startup (dev gets immediate data). Manual triggerSync endpoint available. getLatest handles NULL publishedAt gracefully.</done>
</task>

</tasks>

<verification>
1. Dashboard nav: inspect IconMatch SVG in browser devtools — all child elements have explicit stroke
2. Deck import: import a valid tournament deck (40 main + 12 runes + 1 legend + 1 champion + 3 battlefields) — no "Main deck: 41/40" error
3. News feed: restart API server, check dashboard home — news articles appear
</verification>

<success_criteria>
- Match tab icon visible in dashboard bottom nav
- Valid tournament deck imports show clean validation (no inflated main count)
- Dashboard home displays news articles from riftbound.gg after server restart
</success_criteria>

<output>
After completion, create `.planning/phases/03-points-tracker/03-06-SUMMARY.md`
</output>
