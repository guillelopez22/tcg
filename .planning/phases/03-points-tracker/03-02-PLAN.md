---
phase: 03-points-tracker
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/api/src/modules/match/match.module.ts
  - apps/api/src/modules/match/match.service.ts
  - apps/api/src/modules/match/match.router.ts
  - apps/api/src/modules/match/match.gateway.ts
  - apps/api/src/modules/news/news.module.ts
  - apps/api/src/modules/news/news.service.ts
  - apps/api/src/modules/news/news.router.ts
  - apps/api/src/app.module.ts
  - apps/api/src/main.ts
  - apps/api/src/trpc/trpc.controller.ts
  - apps/api/__tests__/match.service.spec.ts
  - apps/api/__tests__/news.service.spec.ts
autonomous: true
requirements:
  - PTS-01
  - PTS-02
  - PTS-03
  - PTS-06
  - PTS-07
  - PTS-08
  - PLAT-02

must_haves:
  truths:
    - "Match can be created via tRPC returning a 6-char join code"
    - "Guest can join a match by code without authentication"
    - "Socket.IO gateway broadcasts state changes to all clients in a match room"
    - "Match history returns only the authenticated user's past matches"
    - "News scraper fetches and upserts articles on cron schedule"
    - "News endpoint returns articles ordered by publishedAt desc"
  artifacts:
    - path: "apps/api/src/modules/match/match.service.ts"
      provides: "Match CRUD, join, scoring delegation, history"
      exports: ["MatchService"]
    - path: "apps/api/src/modules/match/match.gateway.ts"
      provides: "Socket.IO gateway for real-time match sync"
      exports: ["MatchGateway"]
    - path: "apps/api/src/modules/match/match.router.ts"
      provides: "tRPC procedures for match operations"
      exports: ["MatchRouter"]
    - path: "apps/api/src/modules/news/news.service.ts"
      provides: "News scraping cron + DB queries"
      exports: ["NewsService"]
    - path: "apps/api/src/modules/news/news.router.ts"
      provides: "tRPC procedure for news list"
      exports: ["NewsRouter"]
  key_links:
    - from: "apps/api/src/modules/match/match.gateway.ts"
      to: "apps/api/src/modules/match/match.service.ts"
      via: "DI injection"
      pattern: "this\\.matchService"
    - from: "apps/api/src/modules/match/match.service.ts"
      to: "apps/api/src/modules/match/match-scoring.ts"
      via: "pure function imports"
      pattern: "import.*match-scoring"
    - from: "apps/api/src/modules/news/news.service.ts"
      to: "packages/db/src/schema/news-articles.ts"
      via: "Drizzle query"
      pattern: "newsArticles"
    - from: "apps/api/src/main.ts"
      to: "@nestjs/platform-socket.io"
      via: "IoAdapter"
      pattern: "useWebSocketAdapter"
---

<objective>
Build the complete backend for match sessions and news: NestJS match module (service + tRPC router + Socket.IO gateway), news module (scraper + cron + router), and wire Socket.IO into the existing NestJS app. Install all new dependencies.

Purpose: The match service manages session lifecycle (create, join, score, end, history). The gateway provides real-time sync via Socket.IO rooms. The news module scrapes Riftbound community content. These are the server-side foundations for all match UI and news feed.

Output: Fully functional match and news API endpoints, Socket.IO gateway accepting connections, news cron running.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-points-tracker/03-CONTEXT.md
@.planning/phases/03-points-tracker/03-RESEARCH.md
@.planning/phases/03-points-tracker/03-01-SUMMARY.md

@apps/api/src/app.module.ts
@apps/api/src/main.ts
@apps/api/src/trpc/trpc.service.ts
@apps/api/src/trpc/trpc.controller.ts
@apps/api/src/modules/deck-sync/deck-sync.service.ts
@apps/api/src/modules/deck/deck.service.ts
@apps/api/src/modules/collection/collection.router.ts

<interfaces>
<!-- From Plan 01 outputs — executor reads these from SUMMARY -->
<!-- match-scoring.ts exports: createInitialState, cycleBattlefieldControl, scoreConquest, scoreBeginningPhase, validateWinCondition, advancePhase, advanceTurn -->
<!-- match.schema.ts exports: matchFormatSchema, matchModeSchema, matchCreateSchema, matchJoinSchema, matchStateSchema, matchHistorySchema -->
<!-- match.constants.ts exports: WIN_TARGET_1V1, WIN_TARGET_2V2, WIN_TARGET_FFA, BATTLEFIELDS_1V1, BATTLEFIELDS_2V2, BATTLEFIELDS_FFA, MATCH_PHASES, PLAYER_COLORS -->
<!-- DB: matchSessions, matchPlayers, newsArticles tables -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies + Match module (service + router + gateway)</name>
  <files>
    apps/api/src/modules/match/match.module.ts,
    apps/api/src/modules/match/match.service.ts,
    apps/api/src/modules/match/match.router.ts,
    apps/api/src/modules/match/match.gateway.ts,
    apps/api/src/app.module.ts,
    apps/api/src/main.ts,
    apps/api/src/trpc/trpc.controller.ts,
    apps/api/__tests__/match.service.spec.ts
  </files>
  <action>
    1. Install dependencies:
    ```bash
    pnpm add @nestjs/websockets @nestjs/platform-socket.io socket.io --filter @la-grieta/api
    ```

    2. Create `apps/api/src/modules/match/match.service.ts`:
    - Injectable service with db (Drizzle), redis (ioredis) injected via constructor.
    - `create(userId: string | null, input: MatchCreateInput)`: Generate 6-char nanoid code. Insert into match_sessions with status 'waiting', create match_players rows for initial players with auto-assigned colors from PLAYER_COLORS. Call createInitialState() from match-scoring. Store state in jsonb column. Return { code, sessionId }.
    - `join(userId: string | null, input: MatchJoinInput)`: Look up session by code. If status is 'waiting' or 'active', insert match_player row with next available color. Return session state. If not found, throw NOT_FOUND.
    - `getFullState(code: string)`: Query match_sessions by code, return parsed state jsonb + player list.
    - `applyBattlefieldTap(code: string, data: { battlefieldIndex, playerId })`: Load state, call cycleBattlefieldControl, check if conquest (control changed to a player), if so call scoreConquest. Check validateWinCondition. Save updated state to DB. Return new state.
    - `advancePhase(code: string, playerId: string)`: Load state, call advancePhase. If entering 'B' phase, call scoreBeginningPhase. Save state. Return new state.
    - `advanceTurn(code: string)`: Load state, call advanceTurn from scoring engine. Save. Return.
    - `pauseMatch(code: string)`: Toggle isPaused in state. Save. Return.
    - `endMatch(code: string, winnerId: string | null, reason: 'score' | 'concession')`: Set status='completed', endedAt=now, winnerId. Update match_players with finalScore and isWinner. Return summary.
    - `history(userId: string, input: MatchHistoryInput)`: Query match_sessions joined with match_players where match_players.userId = userId. Order by endedAt desc. Cursor pagination. Return list with format, players, scores, winner, date, duration.
    - All state mutations use UPDATE ... WHERE code = ... RETURNING to prevent race conditions.

    3. Create `apps/api/src/modules/match/match.router.ts`:
    - Follow existing router pattern (deck.router.ts).
    - `create`: optionalAuthProcedure, input(matchCreateSchema), mutation. Guests can create matches.
    - `join`: optionalAuthProcedure, input(matchJoinSchema), mutation.
    - `getState`: publicProcedure, input(z.object({ code: z.string() })), query. For spectators.
    - `history`: rateLimitedProtectedProcedure, input(matchHistorySchema), query.
    - Register router in buildRouter() returning `this.trpc.router({ ... })`.

    4. Create `apps/api/src/modules/match/match.gateway.ts`:
    - `@WebSocketGateway({ cors: { origin: process.env['CORS_ORIGINS']?.split(',') ?? ['http://localhost:3000'], credentials: true }, namespace: '/match' })`
    - Inject MatchService.
    - `handleConnection(client)`: Extract code from handshake query. Join room by code. Send full state via `state:full` event.
    - `handleDisconnect(client)`: Broadcast `player:disconnected` to room.
    - `@SubscribeMessage('battlefield:tap')`: Call matchService.applyBattlefieldTap, broadcast `state:patch` to room.
    - `@SubscribeMessage('phase:advance')`: Call matchService.advancePhase, broadcast.
    - `@SubscribeMessage('turn:advance')`: Call matchService.advanceTurn, broadcast.
    - `@SubscribeMessage('match:pause')`: Call matchService.pauseMatch, broadcast.
    - `@SubscribeMessage('match:end')`: Call matchService.endMatch, broadcast `match:ended`.
    - `@SubscribeMessage('match:undo')`: Restore previous state (keep one-deep undo in state as `previousBattlefields`).
    - Configure 15-second heartbeat: `pingInterval: 15000, pingTimeout: 30000` in gateway decorator options.

    5. Create `apps/api/src/modules/match/match.module.ts`:
    - NestJS module importing CoreModule, providing MatchService, MatchRouter, MatchGateway. Export MatchRouter.

    6. Update `apps/api/src/main.ts`: Add `import { IoAdapter } from '@nestjs/platform-socket.io'` and call `app.useWebSocketAdapter(new IoAdapter(app))` after enableCors().

    7. Update `apps/api/src/app.module.ts`: Import MatchModule.

    8. Update `apps/api/src/trpc/trpc.controller.ts`: Register match router in the merged router (follow existing pattern for deck, collection, etc.).

    9. Create `apps/api/__tests__/match.service.spec.ts`: Test create (returns code), join (returns state), history (filters by userId), applyBattlefieldTap (delegates to scoring). Use mock DB pattern from existing specs (makeMockDb).
  </action>
  <verify>
    <automated>cd /c/Users/guill/OneDrive/Desktop/Cooking/TCG && pnpm --filter @la-grieta/api test -- match.service --reporter=verbose</automated>
  </verify>
  <done>Match service creates sessions with codes, guests can join, gateway broadcasts state changes, match history filters by user, tRPC endpoints registered and callable</done>
</task>

<task type="auto">
  <name>Task 2: News module (scraper + cron + router)</name>
  <files>
    apps/api/src/modules/news/news.module.ts,
    apps/api/src/modules/news/news.service.ts,
    apps/api/src/modules/news/news.router.ts,
    apps/api/src/app.module.ts,
    apps/api/src/trpc/trpc.controller.ts,
    apps/api/__tests__/news.service.spec.ts
  </files>
  <action>
    1. Create `apps/api/src/modules/news/news.service.ts`:
    - Injectable service with db (Drizzle) injected.
    - Private `isSyncing = false` guard (same pattern as DeckSyncService).
    - `@Cron('0 */4 * * *')` decorated `syncCron()`: If syncing, return. Set flag. Try scrapeRiftboundGg(), upsertArticles(). Finally clear flag. Log results.
    - `scrapeRiftboundGg()`: Use axios.get('https://riftbound.gg/blog'). Parse with cheerio. Extract title, excerpt, url (resolve relative to absolute), publishedAt, thumbnailUrl. Return array of article objects. Wrap in try/catch — log error on failure, return empty array (one broken source should not crash cron).
    - `upsertArticles(articles)`: For each article, do INSERT ... ON CONFLICT (url) DO UPDATE SET title, excerpt, thumbnailUrl, publishedAt, scrapedAt=now(). Use Drizzle's onConflictDoUpdate.
    - `getLatest(limit = 20)`: SELECT from news_articles ORDER BY publishedAt DESC NULLS LAST LIMIT. Return array.
    - Private Logger = new Logger(NewsService.name) for structured logging.

    2. Create `apps/api/src/modules/news/news.router.ts`:
    - `getLatest`: publicProcedure (news is visible to everyone), input optional limit (z.number().int().min(1).max(50).default(20)), query.

    3. Create `apps/api/src/modules/news/news.module.ts`:
    - NestJS module importing CoreModule, providing NewsService, NewsRouter. Export NewsRouter.

    4. Update `apps/api/src/app.module.ts`: Import NewsModule (if not already added alongside MatchModule in Task 1).

    5. Update `apps/api/src/trpc/trpc.controller.ts`: Register news router.

    6. Create `apps/api/__tests__/news.service.spec.ts`:
    - Test getLatest returns articles ordered by publishedAt desc.
    - Test upsertArticles deduplicates by URL.
    - Test syncCron guard prevents concurrent runs.
    - Mock axios and cheerio for scraper test.
  </action>
  <verify>
    <automated>cd /c/Users/guill/OneDrive/Desktop/Cooking/TCG && pnpm --filter @la-grieta/api test -- news.service --reporter=verbose</automated>
  </verify>
  <done>News scraper runs on cron, upserts articles by URL, getLatest returns sorted articles, tRPC news.getLatest endpoint registered</done>
</task>

</tasks>

<verification>
- `pnpm --filter @la-grieta/api test -- match.service` passes
- `pnpm --filter @la-grieta/api test -- news.service` passes
- `npx tsc --noEmit -p apps/api/tsconfig.json` compiles
- Match module and news module registered in AppModule
- IoAdapter configured in main.ts
- Match and news routers registered in tRPC controller
</verification>

<success_criteria>
- Match create returns a 6-char code; join by code works for guests (no auth required)
- Socket.IO gateway subscribes to battlefield:tap, phase:advance, turn:advance, match:pause, match:end events and broadcasts state to room
- 15-second heartbeat configured for iOS stability
- Match history endpoint returns only authenticated user's matches with cursor pagination
- News cron runs every 4 hours, scrapes riftbound.gg blog, upserts articles
- News getLatest returns articles sorted by publishedAt desc
- All tests pass, no any types
</success_criteria>

<output>
After completion, create `.planning/phases/03-points-tracker/03-02-SUMMARY.md`
</output>
