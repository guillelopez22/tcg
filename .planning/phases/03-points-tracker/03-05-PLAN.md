---
phase: 03-points-tracker
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - apps/web/src/app/(dashboard)/match/page.tsx
  - apps/web/src/app/(dashboard)/match/[id]/page.tsx
  - apps/web/src/app/(dashboard)/page.tsx
  - apps/web/src/app/(dashboard)/news-feed.tsx
  - apps/web/src/messages/en.json
  - apps/web/src/messages/es.json
autonomous: false
requirements:
  - PTS-08
  - PLAT-02

must_haves:
  truths:
    - "Authenticated users can view their match history with format, scores, opponents, and date"
    - "Match detail view shows turn log, battlefield names, scoring breakdown"
    - "Dashboard home page shows news feed with scraped Riftbound community articles"
    - "News cards show thumbnail, title, source badge, date, and excerpt"
    - "Tapping a news card opens the full article via external link"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/match/page.tsx"
      provides: "Match history list (updated from Plan 03 placeholder)"
      min_lines: 50
    - path: "apps/web/src/app/(dashboard)/match/[id]/page.tsx"
      provides: "Match detail view"
      min_lines: 60
    - path: "apps/web/src/app/(dashboard)/news-feed.tsx"
      provides: "News feed component for dashboard home"
      min_lines: 40
  key_links:
    - from: "apps/web/src/app/(dashboard)/match/page.tsx"
      to: "match.history tRPC"
      via: "useInfiniteQuery"
      pattern: "utils\\.match\\.history"
    - from: "apps/web/src/app/(dashboard)/match/[id]/page.tsx"
      to: "match.getById tRPC"
      via: "useQuery"
      pattern: "utils\\.match\\.getById"
    - from: "apps/web/src/app/(dashboard)/news-feed.tsx"
      to: "news.getLatest tRPC"
      via: "useQuery"
      pattern: "utils\\.news\\.getLatest"
    - from: "apps/web/src/app/(dashboard)/page.tsx"
      to: "apps/web/src/app/(dashboard)/news-feed.tsx"
      via: "component import"
      pattern: "NewsFeed"
---

<objective>
Complete the match history detail view and news feed UI. Add match history detail page with turn log and scoring breakdown. Add news feed component to dashboard home page. Add i18n translations for all new match/news strings. Visual verification checkpoint.

Purpose: PTS-08 requires match history for authenticated users. PLAT-02 requires a news section on the dashboard. This plan completes both remaining requirements and adds the final polish layer with translations.

Output: Match history with detail view, news feed on dashboard, i18n strings, fully working Points Tracker phase.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-points-tracker/03-CONTEXT.md
@.planning/phases/03-points-tracker/03-01-SUMMARY.md
@.planning/phases/03-points-tracker/03-02-SUMMARY.md
@.planning/phases/03-points-tracker/03-03-SUMMARY.md
@.planning/phases/03-points-tracker/03-04-SUMMARY.md

@apps/web/src/app/(dashboard)/page.tsx
@apps/web/src/app/(dashboard)/match/page.tsx
@apps/web/src/messages/en.json
@apps/web/src/messages/es.json

<interfaces>
<!-- tRPC from Plan 02: -->
<!-- match.history: input({ cursor?, limit? }) => { items: MatchHistoryItem[], nextCursor? } -->
<!-- match.getById: protectedProcedure, input({ id: z.string().uuid() }) => full match data including state JSON, player list, format, status, startedAt, endedAt, winnerId -->
<!-- news.getLatest: input({ limit? }) => NewsArticle[] -->
<!-- MatchHistoryItem: { id, format, status, players: [{displayName, finalScore, isWinner, color}], startedAt, endedAt, winnerId, state (with turnHistory) } -->
<!-- NewsArticle: { id, url, title, excerpt, thumbnailUrl, source, publishedAt } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Match history detail + news feed + i18n</name>
  <files>
    apps/web/src/app/(dashboard)/match/page.tsx,
    apps/web/src/app/(dashboard)/match/[id]/page.tsx,
    apps/web/src/app/(dashboard)/news-feed.tsx,
    apps/web/src/app/(dashboard)/page.tsx,
    apps/web/src/messages/en.json,
    apps/web/src/messages/es.json
  </files>
  <action>
    1. Update `apps/web/src/app/(dashboard)/match/page.tsx`:
    - Enhance the match history list from Plan 03. Each match item is now a link to /match/[id] detail page (using the match session UUID).
    - Format badge: colored pill (1v1=blue, 2v2=green, FFA=orange).
    - Show match duration (endedAt - startedAt) formatted as "12m 34s".
    - Win indicator: green checkmark for wins, red X for losses, gray dash for draws.
    - "Concession" tag if applicable (from match state).
    - Cursor-based infinite scroll with "Load More" button.

    2. Create `apps/web/src/app/(dashboard)/match/[id]/page.tsx`:
    - 'use client' component. Fetch match details via `utils.match.getById.useQuery({ id })` where id is the UUID from the URL params. The match.getById procedure is defined in Plan 02 as a protectedProcedure accepting `z.object({ id: z.string().uuid() })`.
    - Display:
      - Header: format badge, date, duration.
      - Players section: all players with final scores, colors, win/loss indicator.
      - Scoring breakdown: total conquest points vs holding points per player (derived from turnHistory events).
      - Battlefield section: names of battlefield cards used (from state.battlefields[].cardName).
      - Turn log: full turn-by-turn history rendered as a timeline. Each turn shows player name, events, points scored.
    - Back button to /match.

    3. Create `apps/web/src/app/(dashboard)/news-feed.tsx`:
    - 'use client' component. Uses utils.news.getLatest.useQuery({ limit: 10 }).
    - Card feed layout per CONTEXT.md: vertical list of news cards.
    - Each card: thumbnail image on left (if available, otherwise source icon placeholder), title (lg-text-primary font-semibold), source badge (small colored pill: 'riftbound.gg'=blue), date (relative format), excerpt (lg-text-secondary line-clamp-2).
    - Entire card is an <a> tag with target="_blank" rel="noopener noreferrer" linking to article URL.
    - Loading skeleton while query fetches.
    - Empty state: "No news yet -- check back later" with newspaper icon.

    4. Update `apps/web/src/app/(dashboard)/page.tsx`:
    - This is the dashboard home page (after login).
    - Import and render NewsFeed component at the top (first thing user sees per CONTEXT.md).
    - Keep any existing dashboard content below the news feed.

    5. Update `apps/web/src/messages/en.json` and `apps/web/src/messages/es.json`:
    - Add match namespace: match.newMatch, match.history, match.noHistory, match.format1v1, match.format2v2, match.formatFfa, match.localMatch, match.syncedMatch, match.playerNames, match.firstPlayer, match.firstTurnReminder, match.createMatch, match.waitingForOpponent, match.joinMatch, match.spectator, match.player, match.matchNotFound, match.reconnecting, match.paused, match.concede, match.concedeConfirm, match.undo, match.nextTurn, match.turnHistory, match.phaseAwaken, match.phaseBeginning, match.phaseChannel, match.phaseDraw, match.wins, match.concession, match.rematch, match.endSession, match.cannotWinWarning, match.duration, match.battlefieldSelection, match.waitingForOpponentSelection, match.revealBattlefields, match.buildDeck, match.deckReady.
    - Add news namespace: news.title, news.noNews, news.readMore.
    - Spanish translations use ES placeholders for now (same pattern as Phase 01 decision).
  </action>
  <verify>
    <automated>cd /c/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Match history detail page shows turn log and scoring breakdown via match.getById, news feed renders on dashboard home, i18n strings added for all match and news text</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Points Tracker end-to-end</name>
  <action>Human verification checkpoint -- verify the full Points Tracker flow, match history, and news feed following the steps below.</action>
  <what-built>Complete Points Tracker: match setup wizard, guest temp deck builder, secret battlefield selection with simultaneous reveal, real-time gameplay board with battlefield control, scoring, ABCD phase tracking, QR join for guests, match history, news feed on dashboard, and Match tab in navigation</what-built>
  <how-to-verify>
    1. Navigate to the app. Verify "Match" tab appears in bottom nav.
    2. Tap Match tab. Verify "New Match" button and (empty) history list appear.
    3. Tap "New Match". Walk through wizard: select 1v1, Synced Match, enter name, pick first player, create.
    4. Verify QR code and 6-char join code are displayed.
    5. On a second device/browser: visit the join URL (or type the code at /match/[code]). Enter a guest name, select Player.
    6. Verify guest deck builder appears with champion-first workflow, zone tabs, and card search. Build a valid temp deck (40 main + 12 runes + 1 champion + at least 2 battlefield cards).
    7. After deck is ready, verify both players see the battlefield selection screen.
    8. On each device: select battlefield card(s). Verify the opponent cannot see your selection before reveal.
    9. After both players confirm selections, verify simultaneous card flip animation plays on BOTH screens.
    10. Verify both screens show the match board with 2 battlefields (showing the revealed card art) and scores at 0/8.
    11. On device A: tap a battlefield. Verify it changes to Player 1 color on BOTH screens.
    12. Advance phase to B (Beginning). Verify +1 point added for controlled battlefield.
    13. Advance through C, D phases. Tap "Next Turn". Verify turn advances.
    14. Play until a player reaches 7 points. Try to win without holding a battlefield. Verify 8th point rule blocks the win with a warning.
    15. Win legitimately. Verify win celebration overlay with confetti appears on both screens.
    16. Verify guest temp deck is cleared from sessionStorage after match ends.
    17. Verify match appears in history on the Match tab. Tap it for detail view with turn log.
    18. Check dashboard home page: verify news feed section appears (may show "No news yet" if scraper hasn't run).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/web/tsconfig.json` compiles
- `pnpm --filter @la-grieta/api test -- match` passes all match tests
- `pnpm --filter @la-grieta/api test -- news` passes all news tests
- Match history page renders for authenticated users
- Match detail page fetches via match.getById (UUID)
- News feed renders on dashboard home
- All i18n keys present in en.json and es.json
</verification>

<success_criteria>
- Match history shows past matches with format, players, scores, date, duration
- Match detail view fetches by UUID via match.getById and shows turn-by-turn log and scoring breakdown
- News feed on dashboard home shows articles from riftbound.gg (or empty state)
- All new UI strings translated (EN + ES placeholders)
- Full end-to-end match flow verified by human: create -> guest builds temp deck -> secret battlefield selection -> simultaneous reveal -> play -> score -> win -> history
</success_criteria>

<output>
After completion, create `.planning/phases/03-points-tracker/03-05-SUMMARY.md`
</output>
