---
phase: 03-points-tracker
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - apps/web/src/app/match/[code]/match-board.tsx
  - apps/web/src/app/match/[code]/battlefield-zone.tsx
  - apps/web/src/app/match/[code]/score-display.tsx
  - apps/web/src/app/match/[code]/turn-controls.tsx
  - apps/web/src/app/match/[code]/turn-log.tsx
  - apps/web/src/app/match/[code]/match-end-overlay.tsx
  - apps/web/src/app/match/[code]/page.tsx
  - apps/web/src/app/match/[code]/join-form.tsx
  - apps/web/src/hooks/use-match-socket.ts
autonomous: true
requirements:
  - PTS-04
  - PTS-05
  - PTS-07

must_haves:
  truths:
    - "User can tap a battlefield to cycle its control state and see the change reflected on both screens"
    - "Scoring auto-applies +1 per controlled battlefield when phase advances to Beginning"
    - "Conquest scoring (+1) triggers visually when battlefield control changes to a player"
    - "ABCD phase indicator shows current phase and advances on tap"
    - "8th point rule blocks invalid wins with a visible warning"
    - "Both players see real-time score updates via Socket.IO"
    - "Win celebration overlay shows on match completion"
  artifacts:
    - path: "apps/web/src/app/match/[code]/match-board.tsx"
      provides: "Main match gameplay view"
      min_lines: 100
    - path: "apps/web/src/app/match/[code]/battlefield-zone.tsx"
      provides: "Tappable battlefield component with card art background"
      min_lines: 50
    - path: "apps/web/src/app/match/[code]/score-display.tsx"
      provides: "Player score display (N/target format)"
      min_lines: 30
    - path: "apps/web/src/hooks/use-match-socket.ts"
      provides: "React hook for Socket.IO match state management"
      exports: ["useMatchSocket"]
  key_links:
    - from: "apps/web/src/hooks/use-match-socket.ts"
      to: "apps/web/src/lib/match-socket.ts"
      via: "getMatchSocket"
      pattern: "getMatchSocket"
    - from: "apps/web/src/app/match/[code]/match-board.tsx"
      to: "apps/web/src/hooks/use-match-socket.ts"
      via: "hook consumption"
      pattern: "useMatchSocket"
    - from: "apps/web/src/app/match/[code]/battlefield-zone.tsx"
      to: "Socket.IO emit"
      via: "battlefield:tap event"
      pattern: "emit.*battlefield:tap"
---

<objective>
Build the full match gameplay board: tappable battlefield zones with card art backgrounds, real-time score display, ABCD phase tracker, turn controls, undo, pause, concession, turn log, and win celebration overlay. Wire everything to Socket.IO for real-time sync.

Purpose: This is the core match experience — two (or more) phones showing the same board state. Battlefield taps, phase advances, and turn changes all flow through Socket.IO. The board must be touch-friendly, work in portrait and landscape, and hide the dashboard nav for full-screen immersion.

Output: Fully playable match board with real-time sync, scoring visualization, turn tracking, and match end handling.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-points-tracker/03-CONTEXT.md
@.planning/phases/03-points-tracker/03-RESEARCH.md
@.planning/phases/03-points-tracker/03-01-SUMMARY.md
@.planning/phases/03-points-tracker/03-02-SUMMARY.md
@.planning/phases/03-points-tracker/03-03-SUMMARY.md

@apps/web/src/lib/match-socket.ts
@apps/web/src/app/match/[code]/page.tsx
@apps/web/src/app/match/[code]/join-form.tsx

<interfaces>
<!-- Socket.IO events from gateway (Plan 02): -->
<!-- Server -> Client: 'state:full' (full MatchState), 'state:patch' (updated MatchState), 'player:disconnected', 'match:ended' -->
<!-- Client -> Server: 'battlefield:tap' ({ battlefieldIndex, playerId }), 'phase:advance' ({ playerId }), 'turn:advance' ({}), 'match:pause' ({}), 'match:end' ({ winnerId, reason }), 'match:undo' ({}) -->
<!-- MatchState shape: { format, phase, currentPlayerId, battlefields[], scores: Record<string,number>, turnNumber, winTarget, winnerId, isPaused, turnHistory[] } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useMatchSocket hook + match board layout + battlefield zones</name>
  <files>
    apps/web/src/hooks/use-match-socket.ts,
    apps/web/src/app/match/[code]/match-board.tsx,
    apps/web/src/app/match/[code]/battlefield-zone.tsx,
    apps/web/src/app/match/[code]/score-display.tsx,
    apps/web/src/app/match/[code]/page.tsx,
    apps/web/src/app/match/[code]/join-form.tsx
  </files>
  <action>
    1. Create `apps/web/src/hooks/use-match-socket.ts`:
    - Custom React hook: `useMatchSocket(code: string)`.
    - State: matchState (MatchState | null), isConnected (boolean), isReconnecting (boolean), players (MatchPlayer[]).
    - On mount: call getMatchSocket(code). Listen for 'state:full' (set full state), 'state:patch' (merge into state), 'connect' (set connected), 'disconnect' (set reconnecting, show banner), 'reconnect' (clear reconnecting), 'match:ended' (set final state).
    - Expose emit helpers: tapBattlefield(bfIndex, playerId), advancePhase(playerId), advanceTurn(), pauseMatch(), endMatch(winnerId, reason), undoAction().
    - On unmount: disconnectMatchSocket().
    - Return { matchState, isConnected, isReconnecting, players, tapBattlefield, advancePhase, advanceTurn, pauseMatch, endMatch, undoAction }.

    2. Create `apps/web/src/app/match/[code]/match-board.tsx`:
    - 'use client' component. Props: { code: string, playerId: string, role: 'player' | 'spectator' }.
    - Uses useMatchSocket(code) hook.
    - Full-screen layout: hide dashboard bottom nav by adding a class or using a context/portal. Small 'Exit' button in top-left corner (absolute positioned).
    - Board-centric layout per CONTEXT.md:
      - 1v1: Player 2 score at top, 2 battlefield zones in center (stacked vertically in portrait, side-by-side in landscape), Player 1 score at bottom.
      - 2v2: Team scores at top/bottom, 3 battlefields in center.
      - FFA: Player scores around edges, 3 battlefields in center.
    - Landscape support: use CSS media query `@media (orientation: landscape)` to adjust layout — battlefields side-by-side, scores on left/right edges.
    - "Reconnecting..." banner (fixed top, lg-bg-warning) when isReconnecting=true.
    - "Paused" overlay when matchState.isPaused=true, showing pause duration timer (elapsed since pause).
    - Spectators see the same board but all tap/control actions are disabled.
    - Player colors: apply border/ring color matching player's assigned color (blue/red/green/yellow) to their score section.

    3. Create `apps/web/src/app/match/[code]/battlefield-zone.tsx`:
    - 'use client' component. Props: { index, control ('uncontrolled'|playerId|'contested'), playerColors: Record<string,string>, cardArt?: string, cardName?: string, onTap, disabled }.
    - Large tappable zone (min-h-32 on mobile). Background: card art image if available (from card database), overlaid with card name text.
    - Visual indicator of control: border/ring color matches controlling player's color. 'uncontrolled' = gray, 'contested' = striped/dashed.
    - On tap: call onTap(index) which triggers Socket.IO emit.
    - Disabled for spectators (pointer-events-none, reduced opacity).
    - Haptic feedback on tap: navigator.vibrate?.(50) on control change.

    4. Create `apps/web/src/app/match/[code]/score-display.tsx`:
    - 'use client' component. Props: { playerName, score, winTarget, color, isCurrentTurn }.
    - Display score as "5/8" format (lg-text-primary text-3xl font-bold).
    - Player name above score.
    - Glow/pulse animation when isCurrentTurn=true.
    - Color accent border matching player color.

    5. Update `apps/web/src/app/match/[code]/page.tsx` and `join-form.tsx`:
    - After successful join (from Plan 03), transition from join-form to match-board component.
    - Pass code, playerId (from join response), and role to MatchBoard.
    - For host arriving from /match/new wizard redirect: auto-connect and render MatchBoard.
  </action>
  <verify>
    <automated>cd /c/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Match board renders with battlefield zones, scores update via Socket.IO, battlefields are tappable with visual control indicators, reconnection banner shows on disconnect, spectators see read-only board</done>
</task>

<task type="auto">
  <name>Task 2: Turn controls, phase tracker, undo, pause, concession, turn log, win celebration</name>
  <files>
    apps/web/src/app/match/[code]/turn-controls.tsx,
    apps/web/src/app/match/[code]/turn-log.tsx,
    apps/web/src/app/match/[code]/match-end-overlay.tsx,
    apps/web/src/app/match/[code]/match-board.tsx
  </files>
  <action>
    1. Create `apps/web/src/app/match/[code]/turn-controls.tsx`:
    - 'use client' component. Props: { phase, currentPlayerId, isMyTurn, onAdvancePhase, onAdvanceTurn, onPause, onEndMatch, onUndo, turnTimer?: number, disabled }.
    - ABCD phase indicator: 4 circles/badges labeled A, B, C, D. Current phase highlighted (lg-bg-accent text-white), others dimmed. Tap current phase badge to advance (calls onAdvancePhase).
    - Phase labels shown below badges: A=Awaken, B=Beginning, C=Channel, D=Draw.
    - "Next Turn" button (lg-btn-primary) — only enabled when phase is 'D' (Draw complete). Calls onAdvanceTurn.
    - Undo button: small icon button, calls onUndo. Tooltip: "Undo last action (5s window)".
    - Pause button: small icon button, calls onPause. Toggles between pause/resume icon.
    - Concede button: small text button "Concede". On tap: show confirmation dialog ("Are you sure? This will end the match as a concession."). On confirm: call onEndMatch(null, 'concession').
    - Optional turn timer: if configured, show countdown in MM:SS format. Warning color when < 30s remaining.
    - When not isMyTurn and not spectator, show "Opponent's turn" text, controls disabled except pause.
    - First-turn reminder: when turnNumber=1 and phase='C', show small banner: "First turn: draw 3 runes instead of 2".

    2. Create `apps/web/src/app/match/[code]/turn-log.tsx`:
    - 'use client' component. Props: { turnHistory: TurnHistoryEntry[] }.
    - Collapsible panel (hidden by default). Toggle button: "Turn History" with chevron icon.
    - When expanded: scrollable list of turn entries. Each entry: "Turn {N}: {player} {event description}".
    - Events like "conquered BF2 (+1)", "holds BF1 (+1 at B phase)", "advanced to Channel".
    - Max height with overflow-y-auto. Auto-scroll to bottom on new entries.

    3. Create `apps/web/src/app/match/[code]/match-end-overlay.tsx`:
    - 'use client' component. Props: { winnerId, winnerName, finalScores, reason, onRematch, onEnd }.
    - Full-screen overlay (fixed inset-0 z-50) with backdrop blur.
    - Winner announcement: large text "{winnerName} Wins!" or "Match Drawn" or "{winnerName} wins by concession".
    - Final scores displayed for all players.
    - Confetti animation: import canvas-confetti, fire on mount with { particleCount: 200, spread: 70, origin: { y: 0.6 } }.
    - Haptic: navigator.vibrate?.([100, 50, 100, 50, 200]) on win.
    - Sound: use AudioContext to play a short victory chime (generate programmatically — simple ascending tones, no audio file needed).
    - Two buttons: "Rematch" (lg-btn-primary) calls onRematch, "End Session" (lg-btn-secondary) calls onEnd (navigates to /match).

    4. Update `apps/web/src/app/match/[code]/match-board.tsx`:
    - Integrate TurnControls below the battlefield zones.
    - Integrate TurnLog as collapsible panel at bottom.
    - Integrate MatchEndOverlay: show when matchState.winnerId is set or match:ended event received.
    - Wire all control actions through useMatchSocket emit helpers.
    - When scoreBeginningPhase triggers (phase transitions to B), show brief "+1" animation on each controlled battlefield (CSS animation, fade out after 1s).
    - When 8th point rule blocks: show toast (sonner) with warning: "Cannot win without holding a battlefield!".
    - Handle 'match:ended' event: stop all interactions, show overlay.
  </action>
  <verify>
    <automated>cd /c/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>ABCD phase tracker visible and interactive, Next Turn button advances turns, undo/pause/concede work, turn log shows history, win celebration overlay with confetti fires on match end, 8th point rule warning shown via toast</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/web/tsconfig.json` compiles
- Match board renders battlefield zones with tap interaction
- ABCD phase indicator advances correctly
- Score display updates in real-time via Socket.IO
- Turn log shows history entries
- Win celebration overlay renders with confetti
</verification>

<success_criteria>
- Full match gameplay loop works: create match -> join -> tap battlefields -> cycle control -> auto-score at Beginning phase -> conquest scoring on control change -> advance phases/turns -> reach win target -> celebration overlay
- Both screens update simultaneously via Socket.IO
- 8th point rule blocks invalid wins with user-visible warning
- Undo, pause, and concession all function
- Board hides dashboard nav for full-screen immersion
- Portrait and landscape layouts supported
</success_criteria>

<output>
After completion, create `.planning/phases/03-points-tracker/03-04-SUMMARY.md`
</output>
