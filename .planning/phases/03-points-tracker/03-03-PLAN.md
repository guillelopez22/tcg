---
phase: 03-points-tracker
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/web/src/app/(dashboard)/match/page.tsx
  - apps/web/src/app/(dashboard)/match/new/page.tsx
  - apps/web/src/app/match/[code]/page.tsx
  - apps/web/src/app/match/[code]/join-form.tsx
  - apps/web/src/app/match/[code]/battlefield-selection.tsx
  - apps/web/src/app/match/[code]/guest-deck-builder.tsx
  - apps/web/src/lib/match-socket.ts
  - apps/web/src/components/dashboard-nav.tsx
  - apps/web/src/components/match-qr-code.tsx
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - PTS-01
  - PTS-02
  - PTS-03
  - PTS-06

must_haves:
  truths:
    - "User can navigate to Match tab from bottom nav"
    - "User can start a new match via the setup wizard selecting format, mode, names, and first player"
    - "QR code and manual join code are displayed after match creation"
    - "Guest can join a synced match by scanning QR or typing the 6-char code without logging in"
    - "Joiner can choose Player or Spectator role"
    - "Each player secretly selects their battlefield card(s) on their own phone before match starts"
    - "Simultaneous card flip reveal occurs when all players have chosen"
    - "Guests can build a full temporary deck (champion-first, zone tabs, card search, format validation) stored in session storage"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/match/page.tsx"
      provides: "Match tab landing page"
      min_lines: 30
    - path: "apps/web/src/app/(dashboard)/match/new/page.tsx"
      provides: "Match setup wizard"
      min_lines: 100
    - path: "apps/web/src/app/match/[code]/page.tsx"
      provides: "Public match join page (outside dashboard auth guard)"
      min_lines: 50
    - path: "apps/web/src/app/match/[code]/battlefield-selection.tsx"
      provides: "Secret battlefield card selection UI per player"
      min_lines: 60
    - path: "apps/web/src/app/match/[code]/guest-deck-builder.tsx"
      provides: "Guest temporary deck builder using session storage"
      min_lines: 80
    - path: "apps/web/src/lib/match-socket.ts"
      provides: "Socket.IO client singleton"
      exports: ["getMatchSocket", "disconnectMatchSocket"]
    - path: "apps/web/src/components/match-qr-code.tsx"
      provides: "QR code display component"
      min_lines: 15
  key_links:
    - from: "apps/web/src/app/(dashboard)/match/new/page.tsx"
      to: "match.create tRPC"
      via: "useMutation"
      pattern: "utils\\.match\\.create"
    - from: "apps/web/src/app/match/[code]/page.tsx"
      to: "match.join tRPC"
      via: "useMutation"
      pattern: "utils\\.match\\.join"
    - from: "apps/web/src/app/match/[code]/battlefield-selection.tsx"
      to: "Socket.IO battlefield:submit"
      via: "emit"
      pattern: "emit.*battlefield:submit"
    - from: "apps/web/src/app/match/[code]/battlefield-selection.tsx"
      to: "Socket.IO battlefield:reveal"
      via: "on listener"
      pattern: "on.*battlefield:reveal"
    - from: "apps/web/src/components/dashboard-nav.tsx"
      to: "apps/web/src/app/(dashboard)/match/page.tsx"
      via: "nav link"
      pattern: "/match"
---

<objective>
Build the match setup flow UI: Match tab in dashboard nav, setup wizard (format/mode/names/first player/battlefield selection), QR code display, the public join page for guests, guest temporary deck builder, and secret battlefield selection with simultaneous reveal. Install web dependencies (socket.io-client, react-qr-code, canvas-confetti).

Purpose: Users need a way to create matches and opponents need a way to join. The setup wizard implements the match creation flow from CONTEXT.md. The join page is a public route (outside dashboard auth guard) so guests can access it without an account. Guests get the full deck editor experience for building a temporary deck. Battlefield selection implements the secret pick + simultaneous reveal per user decision.

Output: Match tab in nav, working setup wizard that creates a match and shows QR, join page that lets guests enter by code, guest temp deck builder, battlefield selection with simultaneous reveal.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-points-tracker/03-CONTEXT.md
@.planning/phases/03-points-tracker/03-RESEARCH.md
@.planning/phases/03-points-tracker/03-01-SUMMARY.md
@.planning/phases/03-points-tracker/03-02-SUMMARY.md

@apps/web/src/app/(dashboard)/layout.tsx
@apps/web/src/components/dashboard-nav.tsx
@apps/web/src/app/(dashboard)/decks/page.tsx
@apps/web/src/lib/trpc.ts

<interfaces>
<!-- tRPC match procedures from Plan 02: -->
<!-- match.create: input(matchCreateSchema) => { code, sessionId } -->
<!-- match.join: input(matchJoinSchema) => session state -->
<!-- match.getState: input({ code }) => full match state + players -->
<!-- match.getById: input({ id: uuid }) => full match data -->
<!-- match.history: input(matchHistorySchema) => paginated match list -->
<!-- Socket.IO events from Plan 02: -->
<!-- Client -> Server: 'battlefield:submit' ({ playerId, cardIds }) -->
<!-- Server -> Client (submitter only): 'battlefield:submitted' (acknowledgment) -->
<!-- Server -> Client (room): 'battlefield:reveal' (revealed state with card art) -->

<!-- From Phase 1.2 (if available) or existing codebase: -->
<!-- deck-card-editor.tsx: Zone-aware editor with champion-first workflow, zone tabs, card search, format validation -->
<!-- card.service.ts: Card search and lookup for battlefield card data -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install web deps + Socket.IO client + QR component + nav tab + guest deck builder</name>
  <files>
    apps/web/src/lib/match-socket.ts,
    apps/web/src/components/match-qr-code.tsx,
    apps/web/src/components/dashboard-nav.tsx,
    apps/web/src/app/(dashboard)/match/page.tsx,
    apps/web/src/app/match/[code]/guest-deck-builder.tsx
  </files>
  <action>
    1. Install web dependencies:
    ```bash
    pnpm add socket.io-client react-qr-code canvas-confetti --filter @la-grieta/web
    pnpm add -D @types/canvas-confetti --filter @la-grieta/web
    ```

    2. Create `apps/web/src/lib/match-socket.ts`:
    - Singleton Socket.IO client. `getMatchSocket(code: string)` creates or returns existing socket connected to `${NEXT_PUBLIC_API_URL?.replace('/api', '')}/match` namespace with query: { code }, withCredentials: true, reconnection: true, reconnectionDelay: 1000, reconnectionDelayMax: 5000, timeout: 20000, transports: ['websocket'] (avoid polling per research).
    - `disconnectMatchSocket()` disconnects and nulls the singleton.
    - Export typed event interfaces for type safety on emit/on. Include battlefield:submit, battlefield:submitted, battlefield:reveal events.

    3. Create `apps/web/src/components/match-qr-code.tsx`:
    - 'use client' component. Props: { code: string }.
    - Uses react-qr-code to render SVG QR of join URL: `${process.env.NEXT_PUBLIC_APP_URL ?? window.location.origin}/match/${code}`.
    - Below QR: display code in monospace font (lg-text-primary font-mono text-2xl tracking-wider).
    - Below that: "or type: lagrieta.gg/match/{code}" in lg-text-muted.
    - Copy-to-clipboard button for the join URL using navigator.clipboard.writeText, sonner toast on copy.

    4. Update `apps/web/src/components/dashboard-nav.tsx`:
    - Add "Match" (or "Play") tab to the bottom nav alongside existing Collection, Decks, Profile tabs.
    - Use a sword/gamepad SVG icon (inline SVG, consistent with existing nav icon style).
    - Link to `/match`.

    5. Create `apps/web/src/app/(dashboard)/match/page.tsx`:
    - 'use client' component. Match tab landing page.
    - Large "New Match" button at top (lg-btn-primary, full width on mobile) linking to /match/new.
    - Below: "Match History" section. Use `utils.match.history.useInfiniteQuery()` (only for authenticated users -- check useAuth()).
    - For unauthenticated users or empty history: show "No matches yet" placeholder.
    - Each history item shows: format badge (1v1/2v2/FFA), opponent names, final score, date (relative via Intl.RelativeTimeFormat or simple date), win/loss indicator, "Concession" tag if applicable.
    - Use lg-card design system classes for each history item.

    6. Create `apps/web/src/app/match/[code]/guest-deck-builder.tsx`:
    - 'use client' component. Props: { onDeckReady: (deck: TempDeck) => void }.
    - Full deck editor experience for guests per user decision: champion-first workflow, zone tabs (main 40, runes 12, champion 1, sideboard), card search, format validation.
    - Reuse or adapt the deck-card-editor.tsx component from Phase 1.2 if available. If Phase 1.2 is not yet built, implement a standalone version with the same UX patterns: (a) step 1: search and select a champion card, (b) step 2: zone-tabbed card browser with search, add cards to zones, (c) real-time format validation (40 main + 12 runes + 1 champion), (d) "Ready" button enabled only when deck is format-valid.
    - No suggestion engine or ownership badges (guest has no collection) per CONTEXT.md. Full format validation still applies.
    - Store temp deck in sessionStorage under key `lagrieta_temp_deck_{matchCode}`. On component mount, check sessionStorage for existing temp deck and restore it (allows page refresh without losing progress).
    - On "Ready" / deck complete: call onDeckReady with the temp deck object containing cardIds grouped by zone and the selected battlefield cards.
    - On match end (detected via Socket.IO 'match:ended' event or navigation away): clear sessionStorage key to discard temp deck.
    - Card data sourced from existing card search tRPC endpoint (card.search / card.getAll) -- guest can browse all cards even without an account since card data is public.
  </action>
  <verify>
    <automated>cd /c/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Match tab visible in dashboard nav, landing page renders with New Match button and history list, QR component renders SVG code, Socket.IO client module exports getMatchSocket/disconnectMatchSocket with battlefield event types, guest deck builder provides full editor experience with session storage persistence</done>
</task>

<task type="auto">
  <name>Task 2: Setup wizard + public join page + battlefield selection UI</name>
  <files>
    apps/web/src/app/(dashboard)/match/new/page.tsx,
    apps/web/src/app/match/[code]/page.tsx,
    apps/web/src/app/match/[code]/join-form.tsx,
    apps/web/src/app/match/[code]/battlefield-selection.tsx
  </files>
  <action>
    1. Create `apps/web/src/app/(dashboard)/match/new/page.tsx`:
    - 'use client' component. Multi-step wizard with local state tracking currentStep (1-6).
    - Step 1 -- Format: Three large tappable cards for 1v1, 2v2, FFA. Each shows format name, battlefield count, win target. Use lg-card with ring-2 ring-accent on selected.
    - Step 2 -- Mode: Two options: "Local Match" (single device icon) and "Synced Match" (two phones icon). Local creates the match immediately; Synced proceeds to QR display.
    - Step 3 -- Player Names: For local mode, input fields for all players (auto-fill from useAuth() displayName if authenticated). For synced mode, only host name (others join via code). In 2v2, show team assignment dropdowns.
    - Step 4 -- First Player: Radio/tap selection of which player goes first. Show reminder text: "First player draws 3 runes in Channel phase (instead of 2)."
    - Step 5 -- Create & Share: Call match.create mutation. On success for synced mode: show MatchQRCode component with the code, "Waiting for opponent..." status. For local mode, redirect directly to battlefield selection step.
    - Step 6 -- Battlefield Selection: Once all players are connected (synced mode) or all names entered (local mode), transition to the BattlefieldSelection component. Each player selects their battlefield cards on their own phone. The match starts after simultaneous reveal.
    - Navigation: Back button on each step, progress dots at top.
    - Use lg-btn-primary for Next/Create, lg-btn-secondary for Back.

    2. Create `apps/web/src/app/match/[code]/battlefield-selection.tsx`:
    - 'use client' component. Props: { code: string, playerId: string, battlefieldCards: CardData[], socket: MatchSocket, onRevealed: (state: MatchState) => void }.
    - Per user decision: "Each player secretly selects their battlefield card(s) on their own phone. Reveal button when all chosen. Simultaneous card flip."
    - Display the player's available battlefield cards from their deck (auth users: from their deck's BF cards via tRPC; guests: from their temp deck built in guest-deck-builder.tsx).
    - Player taps to select the required number of battlefield cards (2 for 1v1, 3 for 2v2/FFA per format).
    - "Confirm Selection" button: emits `battlefield:submit` via Socket.IO with { playerId, cardIds: selectedCardIds }. Button disabled until correct number selected.
    - After submitting: show "Waiting for other players..." with a checkmark on your own selection (cards hidden -- do NOT show what you picked to the opponent's screen).
    - Listen for `battlefield:submitted` (acknowledgment from server for own submission).
    - Listen for `battlefield:reveal` event from server (fired when ALL players have submitted): trigger simultaneous card flip animation -- all battlefield cards animate from face-down (card back) to face-up (card art) using CSS transform rotateY(180deg) transition over 600ms. Play haptic feedback (navigator.vibrate?.(100)).
    - After reveal animation completes (~800ms): call onRevealed(revealedState) to transition to the active match board (Plan 04).
    - For local match mode (single device): show each player's selection sequentially -- Player 1 selects and confirms (screen hides selection), then Player 2 selects and confirms, then both are revealed with the flip animation.

    3. Create `apps/web/src/app/match/[code]/page.tsx`:
    - This is a PUBLIC route (outside (dashboard) group) -- no auth guard.
    - Server component that extracts code from params.
    - Renders JoinForm client component with the code.
    - This page serves dual purpose: join page for new players AND active match view for connected players (determined by checking if already in the session).

    4. Create `apps/web/src/app/match/[code]/join-form.tsx`:
    - 'use client' component. Props: { code: string }.
    - On mount, call match.getState query to check session status.
    - If session not found: show "Match not found" error.
    - If session status is 'waiting' or 'active': show join form with displayName input and role selector (Player/Spectator radio).
    - For guests joining as Player: after entering name, show the GuestDeckBuilder component. Guest must build a valid temp deck before they can join the match. The join flow becomes: enter name -> select role -> (if Player) build temp deck -> join match -> battlefield selection.
    - For guests joining as Spectator: no deck building needed, proceed directly to join.
    - Submit calls match.join mutation. On success, establish Socket.IO connection via getMatchSocket(code) and transition to BattlefieldSelection (if match hasn't started yet) or directly to match board (if match is already active).
    - If session status is 'completed': show match summary (scores, winner).
    - Handle "Reconnecting..." banner: listen for socket disconnect/reconnect events, show lg-banner-warning banner.
  </action>
  <verify>
    <automated>cd /c/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Setup wizard creates match through all steps including battlefield selection, QR code shown after creation, public join page lets guests build temp deck and enter name and join as Player or Spectator, battlefield selection implements secret pick per player with simultaneous card flip reveal animation, Socket.IO connection established on join</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/web/tsconfig.json` compiles
- Match tab appears in bottom nav
- /match page renders with New Match button
- /match/new wizard flows through all steps including battlefield selection
- /match/[code] public join page renders without auth
- QR code renders with correct join URL
- Guest deck builder renders with champion-first workflow and zone tabs
- Battlefield selection emits battlefield:submit and listens for battlefield:reveal
- Simultaneous card flip animation plays on reveal
</verification>

<success_criteria>
- Dashboard bottom nav shows Match/Play tab
- Setup wizard: user selects format (1v1/2v2/FFA), mode (Local/Synced), enters names, picks first player, creates match, sees QR + code
- Public join page: guest enters name, selects Player/Spectator, builds temp deck (if Player), joins match
- Guest temp deck: full editor with champion-first workflow, zone tabs, card search, format validation, stored in sessionStorage, discarded after match ends
- Battlefield selection: each player secretly picks battlefield cards on own phone, server holds until all submit, simultaneous card flip animation on all screens
- Socket.IO connection established on join with reconnect banner support
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-points-tracker/03-03-SUMMARY.md`
</output>
