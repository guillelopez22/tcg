---
phase: 01.2-smart-deck-builder
verified: 2026-03-12T00:00:00Z
status: passed
score: 26/26 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Open /decks and verify each deck card shows Draft/Complete badge and buildability percentage"
    expected: "Green 'Complete' badge or amber 'Draft' badge visible; 'You own X/Y (Z%)' text colored green/amber/red"
    why_human: "Cannot verify rendering without running the browser"
  - test: "Open a deck editor and verify zone tabs (Main/Runes/Champion/Sideboard), card search auto-filtering, ownership badges, wantlist prompt when adding unowned card, swap prompt when zone full, and validity status bar"
    expected: "Zone tabs switch correctly; search hides Legends in Main tab, only shows Runes in Rune tab; 'Own: N' badge on each card; toast with 'Add to wantlist' on unowned add; swap UI when zone at capacity; green/red validity bar"
    why_human: "Multi-step interactive UI flow requiring browser"
  - test: "Switch to Suggested tab in editor — toggle Owned first / Best fit and verify suggestion cards with reason tags render"
    expected: "Reason tag pills ('Meta pick', 'Curve filler', 'Domain match', 'Synergy: X', 'Owned') shown on cards; toggle changes ranking"
    why_human: "Requires live API call to trpc.deck.suggest"
  - test: "Click Auto-complete — verify preview modal appears before cards are added"
    expected: "Preview modal lists cards to be added with reason tags; Confirm fills zone; Cancel dismisses"
    why_human: "Multi-step interactive UI"
  - test: "Create new deck via wizard (Legend path) — verify 'Build with my cards' and 'Build best deck' step appears after legend selection"
    expected: "Two buttons on choose-build-mode step; each produces different card selection in the created deck"
    why_human: "Requires UI interaction and live suggestion endpoint"
  - test: "Open deck detail — switch to Analytics tab and verify 5 chart sections render correctly"
    expected: "Energy Curve BarChart, Domain Distribution PieChart, Card Type Breakdown, Rarity Distribution, Market Value all visible"
    why_human: "Requires browser rendering of recharts components"
  - test: "On deck detail buildability section — click 'Add X missing cards to wantlist' button"
    expected: "Calls wishlist.toggle for each missing card; shows toast 'Added N cards to wantlist'"
    why_human: "Requires authenticated session and live API"
---

# Phase 01.2: Smart Deck Builder Verification Report

**Phase Goal:** Upgrade the existing deck builder into a smart, collection-aware editor with zone-based format validation (40 main + 12 runes + 1 champion + sideboard), real-time analytics (energy curve, domain distribution, card type breakdown, rarity, market value), ownership integration (own badges, buildability %, wantlist integration), and intelligent card suggestions (synergy-based with domain + curve + meta co-occurrence + text keyword analysis)
**Verified:** 2026-03-12T00:00:00Z
**Status:** passed (automated checks pass; human verification needed for interactive UI flows)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | deck_cards table has a zone column | VERIFIED | `packages/db/src/schema/decks.ts` line 27: `zone: varchar('zone', { length: 20 }).notNull().default('main')` |
| 2 | decks table has a status column with draft/complete | VERIFIED | `packages/db/src/schema/decks.ts` line 14: `status: varchar('status', { length: 20 }).notNull().default('draft')` |
| 3 | cards table has a keywords TEXT[] column | VERIFIED | `packages/db/src/schema/cards.ts` line 25: `keywords: text('keywords').array().default([])` |
| 4 | Unique index on deck_cards is (deckId, cardId, zone) | VERIFIED | `packages/db/src/schema/decks.ts` line 31: `uniqueIndex('idx_deck_cards_deck_card_zone').on(table.deckId, table.cardId, table.zone)` |
| 5 | Zod schemas enforce zone-aware card entries with correct per-zone limits | VERIFIED | `packages/shared/src/schemas/deck.schema.ts`: `deckCardEntrySchema` has `zone: z.enum(DECK_ZONES)`, quantity max is `MAX_COPIES_PER_CARD` (3), `.max(61)` on cards arrays |
| 6 | Card constants reflect correct Riftbound copy limits | VERIFIED | `packages/shared/src/constants/card.constants.ts`: `MAX_COPIES_PER_CARD=3`, `MAIN_DECK_SIZE=40`, `RUNE_DECK_SIZE=12`, `CHAMPION_COUNT=1`, `SIDEBOARD_SIZE=8`, `SIGNATURE_TYPES` all exported |
| 7 | Shared validateDeckFormat utility exported and pure | VERIFIED | `packages/shared/src/utils/validate-deck-format.ts` — pure function, no Node/browser deps, exported from `packages/shared/src/index.ts` |
| 8 | deckSuggestSchema and deckBuildabilitySchema exported | VERIFIED | `packages/shared/src/schemas/deck.schema.ts` lines 53-72 |
| 9 | Migration script exists with all 5 idempotent SQL statements | VERIFIED | `tools/seed/src/migrate-01.2-01.cjs` — all 5 ALTER/CREATE statements with IF NOT EXISTS guards |
| 10 | setCards validates zone-specific limits and computes status | VERIFIED | `apps/api/src/modules/deck/deck.service.ts` — calls `validateDeckFormat`, sets status 'complete'/'draft' inside transaction |
| 11 | getBuildability returns owned, total, pct, missingCardIds | VERIFIED | `deck.service.ts` lines 452-496 — full implementation querying collections |
| 12 | suggest() returns SuggestedCard[] with score and reasonTag | VERIFIED | `deck.service.ts` lines 498-709 — full scoring logic: domain match (+3), curve filler (+2), co-occurrence (+2), keyword synergy (+1/kw), owned_first boost (+5), Redis cached 10 min |
| 13 | deck.router.ts exposes suggest and buildability tRPC procedures | VERIFIED | `apps/api/src/modules/deck/deck.router.ts` lines 57-63 |
| 14 | keyword-tag.service.ts exists with @Cron decorator and parseKeywords | VERIFIED | `apps/api/src/modules/deck/keyword-tag.service.ts` — `@Cron('0 7 * * *')`, `parseKeywords` exported as pure function |
| 15 | KeywordTagModule imported in trpc.module.ts | VERIFIED | `apps/api/src/trpc/trpc.module.ts` line 18 imports `KeywordTagModule` |
| 16 | computeAnalytics pure function exists in shared | VERIFIED | `packages/shared/src/utils/compute-analytics.ts` — groups energyCost 0-7 individually and 8+, handles null as 0, null domain as 'Neutral', respects quantity |
| 17 | computeAnalytics exported from shared index | VERIFIED | `packages/shared/src/index.ts` line: `export * from './utils/compute-analytics'` |
| 18 | deck-analytics.spec.ts covers computeAnalytics behavior | VERIFIED | `apps/api/__tests__/deck-analytics.spec.ts` — 6 test cases matching contract: buckets, null energyCost, null domain, empty array, quantities, 8+ grouping |
| 19 | keyword-tag.service.spec.ts covers parseKeywords | VERIFIED | `apps/api/__tests__/keyword-tag.service.spec.ts` — 11 tests covering all keyword types, null description, deduplication, complex descriptions |
| 20 | deck.service.spec.ts has zone validation tests | VERIFIED | `apps/api/__tests__/deck.service.spec.ts` — tests at line 759+: zone validation group covers 40+12+1 valid deck, sideboard rules, copy limits |
| 21 | deck-card-editor.tsx is substantive (>500 lines), zone-tabbed, uses shared validateDeckFormat | VERIFIED | 1105 lines; imports `validateDeckFormat`, `DECK_ZONES`, `getZoneForCardType`, `MAIN_DECK_SIZE`; `activeZone` state; zone tabs rendered via `DECK_ZONES.map` |
| 22 | Editor has suggestions tab with owned-first/best-fit toggle | VERIFIED | `deck-card-editor.tsx`: `searchPanel` state, `trpc.deck.suggest.useQuery` enabled when `searchPanel === 'suggested'`, mode toggle at lines 756-766 |
| 23 | Editor has auto-complete with preview | VERIFIED | `autoCompletePreview` state, modal at line 1038, preview before confirm |
| 24 | Editor shows wantlist prompt and swap prompt | VERIFIED | `wishlistMutation` (lines 269-271), swap prompt state and rendering at lines 957+ |
| 25 | deck-wizard.tsx has choose-build-mode step with "Build with my cards" / "Build best deck" | VERIFIED | `WizardStep` type includes `'choose-build-mode'`, rendered at line 853+, buttons at lines 887/908 |
| 26 | deck-detail.tsx and deck-list.tsx show buildability % and Draft/Complete badge | VERIFIED | `deck-list.tsx`: `DeckBuildability` component calls `trpc.deck.buildability`, `DeckStatusBadge` component; `deck-detail.tsx`: imports `DeckAnalytics`, `BuildabilitySection` calls `trpc.deck.buildability`, status badge in header |

**Score:** 26/26 truths verified

---

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `packages/db/src/schema/decks.ts` | VERIFIED | zone + status columns, updated unique index |
| `packages/db/src/schema/cards.ts` | VERIFIED | keywords TEXT[] column at line 25 |
| `packages/shared/src/schemas/deck.schema.ts` | VERIFIED | deckCardEntrySchema with zone, deckSuggestSchema, deckBuildabilitySchema, all types exported |
| `packages/shared/src/constants/card.constants.ts` | VERIFIED | All deck constants exported; getZoneForCardType function |
| `packages/shared/src/utils/validate-deck-format.ts` | VERIFIED | Pure function, all zone checks, copy limits, signature limits |
| `packages/shared/src/utils/compute-analytics.ts` | VERIFIED | Pure function, energy curve bucketing, domain distribution |
| `tools/seed/src/migrate-01.2-01.cjs` | VERIFIED | 5 idempotent SQL statements |
| `apps/api/src/modules/deck/deck.service.ts` | VERIFIED | zone validation, buildability, suggest with scoring, Redis cache |
| `apps/api/src/modules/deck/deck.router.ts` | VERIFIED | suggest + buildability procedures wired |
| `apps/api/src/modules/deck/keyword-tag.service.ts` | VERIFIED | @Cron daily, parseKeywords pure fn |
| `apps/api/src/modules/deck/keyword-tag.module.ts` | VERIFIED | Provides KeywordTagService with DB_TOKEN injection |
| `apps/api/__tests__/deck.service.spec.ts` | VERIFIED | Zone validation test group |
| `apps/api/__tests__/deck-analytics.spec.ts` | VERIFIED | 6 computeAnalytics tests |
| `apps/api/__tests__/keyword-tag.service.spec.ts` | VERIFIED | 11 parseKeywords tests |
| `apps/web/src/app/(dashboard)/decks/[id]/deck-card-editor.tsx` | VERIFIED | 1105 lines; zone tabs, validity bar, ownership badges, swap prompt, suggestion tab, auto-complete, mini analytics |
| `apps/web/src/app/(dashboard)/decks/[id]/deck-analytics.tsx` | VERIFIED | 270 lines; imports computeAnalytics, 5 chart sections |
| `apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx` | VERIFIED | Analytics tab, BuildabilitySection with add-missing-to-wantlist, status badge |
| `apps/web/src/app/(dashboard)/decks/deck-list.tsx` | VERIFIED | DeckBuildability component, DeckStatusBadge component |
| `apps/web/src/app/(dashboard)/decks/deck-wizard.tsx` | VERIFIED | choose-build-mode step with owned_first/best_fit split |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `deck.schema.ts` | `card.constants.ts` | imports DECK_ZONES | WIRED | Line 2: `import { DECK_ZONES, MAX_COPIES_PER_CARD }` |
| `validate-deck-format.ts` | `card.constants.ts` | imports all zone size constants | WIRED | Imports MAX_COPIES_PER_CARD, MAX_SIGNATURE_COPIES, MAIN_DECK_SIZE, RUNE_DECK_SIZE, CHAMPION_COUNT, SIDEBOARD_SIZE, SIGNATURE_TYPES |
| `deck.service.ts` | `validate-deck-format.ts` | imports validateDeckFormat | WIRED | Line 21: `validateDeckFormat` imported from `@la-grieta/shared` |
| `deck.service.ts` | `card.constants.ts` | imports MAX_COPIES_PER_CARD, SIGNATURE_TYPES | WIRED | Lines 22-25: MAX_COPIES_PER_CARD, SIGNATURE_TYPES, getZoneForCardType imported |
| `deck.router.ts` | `deck.schema.ts` | imports deckSuggestSchema, deckBuildabilitySchema | WIRED | Lines 12-14: both schemas imported and used in procedure definitions |
| `keyword-tag.service.ts` | `cards.ts` (keywords column) | writes to cards.keywords | WIRED | Line 71: `.set({ keywords })` in DB update |
| `keyword-tag.module.ts` | `trpc.module.ts` | registered | WIRED | trpc.module.ts imports KeywordTagModule |
| `deck-card-editor.tsx` | `@la-grieta/shared` | imports validateDeckFormat, getZoneForCardType, constants | WIRED | Lines 8-24 — all constants and utilities imported and used |
| `deck-card-editor.tsx` | `trpc.deck.suggest` | useQuery for suggestions | WIRED | Line 231-233: trpc.deck.suggest.useQuery |
| `deck-card-editor.tsx` | `trpc.deck.setCards` | useMutation for saving | WIRED | Line 259: trpc.deck.setCards.useMutation |
| `deck-detail.tsx` | `deck-analytics.tsx` | imports DeckAnalytics | WIRED | Line 12: `import { DeckAnalytics } from './deck-analytics'`; used at line 268 |
| `deck-list.tsx` | `trpc.deck.buildability` | useQuery per deck | WIRED | Line 18: trpc.deck.buildability.useQuery in DeckBuildability component |
| `deck-analytics.spec.ts` | `compute-analytics.ts` | imports computeAnalytics from shared | WIRED | Line 2: `import { computeAnalytics } from '../../packages/shared/src/utils/compute-analytics'` |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| DECK-01 | 01.2-02, 01.2-03 | User can build a deck using champion-first workflow | SATISFIED | Wizard pick-legend step flows to choose-build-mode; editor champion zone + zone-type filtering forces champion selection; getZoneForCardType maps 'Legend' -> 'champion' |
| DECK-02 | 01.2-01, 01.2-02, 01.2-03, 01.2-04 | Deck builder validates format in real time (40 main + 12 runes + 1 champion) | SATISFIED | validateDeckFormat shared util; editor validity bar calls it on every state change; server uses same function in setCards; deck status computed from validation result |
| DECK-04 | 01.2-02, 01.2-03, 01.2-04 | User can view deck analytics (energy curve, domain distribution) | SATISFIED | computeAnalytics in shared; deck-analytics.tsx renders 5 chart sections (energy curve, domain, card type, rarity, market value); mini charts in editor update in real-time |

No orphaned requirements: REQUIREMENTS.md marks DECK-01, DECK-02, DECK-04 as Complete for Phase 2. No additional phase-2 DECK requirements were claimed by plans but left unimplemented.

---

### Anti-Patterns Found

No significant blockers detected. Scanned all modified files for TODO/FIXME/placeholder patterns and empty implementations.

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `deck-analytics.spec.ts` | Uses `computeAnalytics` imported from `../../packages/shared/src/utils/compute-analytics` (relative path into another package) | Info | Works but unconventional; production usage via `@la-grieta/shared` is correct |

---

### Human Verification Required

#### 1. Deck List: Status Badge and Buildability Percentage

**Test:** Log in, navigate to `/decks`, view "My Decks" tab.
**Expected:** Each deck card shows either a green "Complete" badge or an amber "Draft" badge. Below the deck name shows "You own X/Y (Z%)" with color-coded percentage.
**Why human:** CSS rendering and badge placement cannot be verified programmatically.

#### 2. Deck Editor: Zone Tabs and Card Search Auto-Filtering

**Test:** Click Edit on a deck. Observe zone tabs at top of card list panel. Switch between Main, Runes, Champion, Sideboard.
**Expected:** Active tab highlighted. Card search results update: Main hides Legends and Runes; Rune tab shows only Rune cards; Champion tab shows only Legends. Card count badges on tabs (e.g., "Main 35/40") update as cards are added.
**Why human:** Requires live rendering and card database to verify filtering.

#### 3. Deck Editor: Ownership Badges, Wantlist Prompt, Swap Prompt

**Test:** In the editor, search for a card you do not own and click Add. Then fill a zone to capacity and try to add another card.
**Expected:** "Own: 0" badge appears on unowned cards in muted color. Adding an unowned card shows a toast "You don't own this card." with "Add to wantlist" action. When zone is full, an inline swap prompt appears listing existing cards with Replace buttons.
**Why human:** Requires live collection data and interactive toast/prompt rendering.

#### 4. Deck Editor: Validity Status Bar

**Test:** In the editor with an incomplete deck, observe the top status bar. Fill the deck to 40 main + 12 runes + 1 champion.
**Expected:** Red bar with error list for incomplete deck; green "Valid" bar when deck meets all zone requirements.
**Why human:** Visual rendering and real-time state tracking requires browser.

#### 5. Deck Editor: Suggestions Tab

**Test:** Switch to the "Suggested" tab in the left panel. Toggle between "Owned first" and "Best fit". Click a reason tag pill on a suggestion card.
**Expected:** Suggestion cards appear with reason tag pills (Meta pick, Curve filler, Domain match, Synergy:X, Owned). Toggling mode changes ranking. Clicking a tag expands reasonDetail text.
**Why human:** Requires live trpc.deck.suggest API call and rendering.

#### 6. Deck Editor: Auto-Complete

**Test:** When active zone has empty slots, click the "Auto-complete" button.
**Expected:** A preview modal appears listing which cards would fill the remaining slots with their reason tags. Clicking Confirm adds them; Cancel dismisses without changes.
**Why human:** Interactive modal flow requires browser.

#### 7. Wizard: Build with my cards / Build best deck

**Test:** Create a new deck via the wizard, select "Build Around Legend", pick a legend.
**Expected:** A new "Build Style" step appears with two buttons: "Build with my cards" and "Build best deck". Each produces a different card selection in the resulting deck (owned_first vs best_fit scoring).
**Why human:** Requires live API interaction and visual step rendering.

#### 8. Deck Detail: Analytics Tab

**Test:** Navigate to a deck detail page. Click the "Analytics" tab.
**Expected:** Five sections render: Energy Curve (bar chart), Domain Distribution (pie chart), Card Type Breakdown (bar/horizontal chart), Rarity Distribution (pie chart), Estimated Market Value (text).
**Why human:** Requires recharts rendering in browser.

#### 9. Deck Detail: Buildability and Add Missing to Wantlist

**Test:** On a deck you do not fully own, verify the buildability section. Click "Add X missing cards to wantlist."
**Expected:** Shows "You own X/Y cards (Z%)" with a progress bar. If missing cards exist, button is enabled. Clicking it shows a sonner toast "Added N cards to wantlist" and calls wishlist.toggle for each missing card.
**Why human:** Requires authenticated session and live wishlist API.

---

### Gaps Summary

No blocking gaps. All 26 automated must-haves pass:

- Plan 01 data layer: DB schema (zone, status, keywords), migration script, Zod schemas, constants, and validateDeckFormat utility are all fully implemented and exported from `@la-grieta/shared`.
- Plan 02 service layer: DeckService uses shared validateDeckFormat, computes deck status, implements getBuildability and suggest() with multi-signal scoring (domain, curve, co-occurrence, keywords, owned_first boost), Redis caching. KeywordTagService cron and parseKeywords are tested. deck.router.ts exposes all procedures.
- Plan 03 editor: deck-card-editor.tsx (1105 lines) is fully implemented — zone tabs, validity bar using shared validateDeckFormat (no duplication), ownership badges, wantlist prompt, swap prompt, suggestion tab with owned-first/best-fit toggle, mini analytics charts, auto-complete with preview. deck-wizard.tsx has the choose-build-mode step.
- Plan 04 list/detail: deck-list.tsx shows buildability and status badges. deck-detail.tsx shows analytics tab with DeckAnalytics component and buildability section with add-missing-to-wantlist. deck-analytics.tsx (270 lines) renders 5 chart sections using the tested computeAnalytics function from shared.

The only items requiring human verification are visual rendering and interactive UI flows that cannot be confirmed statically.

---

_Verified: 2026-03-12T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
