---
phase: 01.2-smart-deck-builder
plan: 04
type: execute
wave: 3
depends_on: ["01.2-01", "01.2-02"]
files_modified:
  - apps/web/src/app/(dashboard)/decks/deck-list.tsx
  - apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx
  - apps/web/src/app/(dashboard)/decks/[id]/deck-analytics.tsx
autonomous: false
requirements:
  - DECK-02
  - DECK-04

must_haves:
  truths:
    - "Deck list shows buildability percentage for each deck"
    - "Deck list shows Draft/Complete status badge on each deck"
    - "Deck detail page shows full analytics: energy curve, domain distribution, card type breakdown, rarity distribution, market value"
    - "Deck detail page shows buildability percentage with Add missing to wantlist button"
    - "deck-analytics.tsx computeAnalytics uses the same pure function tested in deck-analytics.spec.ts"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/decks/deck-list.tsx"
      provides: "Buildability % and draft/complete badge per deck card"
    - path: "apps/web/src/app/(dashboard)/decks/[id]/deck-analytics.tsx"
      provides: "Full analytics component with 5 chart sections, using tested computeAnalytics function"
      min_lines: 80
    - path: "apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx"
      provides: "Analytics tab/section + buildability section with add-missing-to-wantlist"
  key_links:
    - from: "apps/web/src/app/(dashboard)/decks/deck-list.tsx"
      to: "/api via tRPC"
      via: "trpc.deck.buildability.useQuery for each deck"
      pattern: "trpc\\.deck\\.buildability"
    - from: "apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx"
      to: "apps/web/src/app/(dashboard)/decks/[id]/deck-analytics.tsx"
      via: "imports DeckAnalytics component"
      pattern: "DeckAnalytics"
---

<objective>
Deck list and detail page enhancements: buildability percentage, draft/complete status, full analytics, and wantlist integration.

Purpose: Surface collection awareness and deck quality metrics across the deck browsing experience. Buildability % motivates collection completion and drives wantlist usage. Full analytics give players data-driven deck insights. Draft/Complete status helps users track deck readiness.

Output: Updated deck-list.tsx with buildability and status badges, new deck-analytics.tsx component with 5 chart sections, updated deck-detail.tsx with analytics tab and buildability section.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-smart-deck-builder/01.2-CONTEXT.md
@.planning/phases/01.2-smart-deck-builder/01.2-RESEARCH.md
@.planning/phases/01.2-smart-deck-builder/01.2-01-SUMMARY.md
@.planning/phases/01.2-smart-deck-builder/01.2-02-SUMMARY.md

<interfaces>
<!-- tRPC procedures available -->
```
trpc.deck.list.useQuery({ limit })        -> PaginatedResult<DeckWithCover>  (DeckWithCover now includes status: string)
trpc.deck.getById.useQuery({ id })        -> DeckWithCards  (cards now include zone, card includes energyCost)
trpc.deck.buildability.useQuery({ deckId }) -> { owned: number, total: number, pct: number, missingCardIds: string[] }
trpc.wishlist.addBulk or trpc.wishlist.toggle for wantlist integration
```

From apps/web/src/app/(dashboard)/decks/deck-list.tsx (current):
```typescript
// Tabbed layout: My Decks | Trending
// Each deck card shows cover image, name, domain, card count
// Uses trpc.deck.list.useInfiniteQuery for My Decks
```

From apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx (current):
```typescript
// Shows deck name, description, card list, edit button
// Uses trpc.deck.getById.useQuery
```

From collection stats recharts pattern:
```typescript
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, PieChart, Pie, Cell } from 'recharts';
```

DOMAIN_COLORS and RARITY_COLORS from design tokens (defined inline in existing components).

<!-- computeAnalytics pure function (tested in apps/api/__tests__/deck-analytics.spec.ts from Plan 02 Task 3) -->
The computeAnalytics function must be implemented as a pure function that can be imported and tested.
Preferred location: define it in a shared utility or inline in deck-analytics.tsx but ensure it matches
the test expectations from deck-analytics.spec.ts:
- Groups energyCost into buckets 0-7, 8+ (null energyCost treated as 0)
- Aggregates domain distribution with null -> "Neutral"
- Multiplies by quantity for accurate counts
- Returns empty distributions for empty card arrays
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deck list buildability and status badges</name>
  <files>apps/web/src/app/(dashboard)/decks/deck-list.tsx</files>
  <action>
1. Update the "My Decks" card rendering in `deck-list.tsx`:

**Draft/Complete status badge:**
- Read `deck.status` from the list response (now available after Plan 01 schema + Plan 02 service)
- Show a badge on each deck card:
  - "Complete" in green (bg-emerald-500/20 text-emerald-400) with checkmark icon
  - "Draft" in amber (bg-amber-500/20 text-amber-400) with pencil/edit icon
- Position: top-right corner of the deck card or inline next to deck name

**Buildability percentage:**
- For each deck in the list, call `trpc.deck.buildability.useQuery({ deckId: deck.id })`
- IMPORTANT: This makes N queries for N decks. To keep it performant, consider batching or only loading for visible decks. If this is too many queries, alternative: add a `buildabilityPct` field to the deck list response in the service (this would be a minor addition to deck.service.ts list() method -- join collections table).
- Preferred approach per research: show buildability inline. If query count is a concern, batch by adding buildability to the list query in the service. For now, use individual queries with `enabled: !!deck.id` to lazy-load.
- Display: "You own 32/53 (60%)" below the deck name in muted text
- Color the percentage: green (>=80%), amber (40-79%), red (<40%)

**Layout adjustments:**
- Ensure deck cards don't get too cluttered -- status badge and buildability should be compact
- Use `lg-*` design system classes where available
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>Each deck in "My Decks" shows a Draft/Complete badge and buildability percentage with color coding. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Full analytics component + deck detail buildability and wantlist</name>
  <files>apps/web/src/app/(dashboard)/decks/[id]/deck-analytics.tsx, apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx</files>
  <action>
1. Create `apps/web/src/app/(dashboard)/decks/[id]/deck-analytics.tsx`:
   - Client component receiving `cards: DeckCardWithCard[]` as prop (from deck.getById)
   - `computeAnalytics(cards)` pure function -- this MUST match the behavior tested in `apps/api/__tests__/deck-analytics.spec.ts` (created in Plan 02 Task 3). Either:
     a. Import computeAnalytics from a shared location if Plan 02 Task 3 placed it there, OR
     b. Implement it inline but ensure it matches the tested contract:
        - Energy curve: group by energyCost (null treated as 0), bucket 8+ together, multiply by quantity
        - Domain distribution: group by card.domain (null -> "Neutral"), multiply by quantity
        - Card type breakdown: group by card.cardType, multiply by quantity
        - Rarity distribution: group by card.rarity, multiply by quantity
        - Market value: sum of (card price * quantity) if price data available, else "N/A"
   - Render 5 sections:
     a. **Energy Curve** -- recharts BarChart, 200px height, bars colored purple (#7c3aed), X axis shows cost 0-8+, Y axis shows count
     b. **Domain Distribution** -- recharts PieChart (donut), 200px, segments colored by DOMAIN_COLORS, label showing domain name + count
     c. **Card Type Breakdown** -- recharts horizontal BarChart or simple colored bars, showing Unit, Spell, Gear, etc.
     d. **Rarity Distribution** -- recharts PieChart or bar chart, segments colored by rarity (Common=zinc, Uncommon=green, Rare=blue, Epic=purple, Showcase=amber)
     e. **Estimated Market Value** -- simple text display: "~${totalValue}" or "Price data unavailable"
   - Use `ResponsiveContainer` for all charts
   - Use dark theme tooltip styling (match collection stats pattern: `contentStyle={{ background: '#18181b', border: '1px solid #3f3f46' }}`)

2. Update `apps/web/src/app/(dashboard)/decks/[id]/deck-detail.tsx`:

**Analytics tab/section:**
- Add a tabbed section or accordion below the card list: "Cards" | "Analytics"
- When "Analytics" tab is active, render `<DeckAnalytics cards={deck.cards} />`
- All analytics compute client-side from the cards already loaded by getById (no extra API call per user decision)

**Buildability section:**
- Call `trpc.deck.buildability.useQuery({ deckId: deck.id })` (only for authenticated users viewing their own deck)
- Display: "You own {owned}/{total} cards ({pct}%)"
- Progress bar showing ownership percentage (colored green/amber/red)
- "Add {missingCount} missing to wantlist" button:
  - On click: iterate over `missingCardIds` and call `trpc.wishlist.toggle.mutate({ cardId, type: 'want' })` for each
  - Or use `trpc.wishlist.addBulk` if available
  - Show sonner toast on completion: "Added {count} cards to wantlist"
  - Disable button while mutation is in progress
  - Only show this button if there are missing cards

**Draft/Complete badge on detail page:**
- Show the same status badge as the list page, in the deck header area
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/web/tsconfig.json</automated>
  </verify>
  <done>deck-analytics.tsx renders 5 chart sections using computeAnalytics that matches test expectations. Deck detail shows analytics tab, buildability percentage with progress bar, and "Add missing to wantlist" button. Status badge on detail header.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete smart deck builder</name>
  <what-built>
Complete smart deck builder: zone-tabbed editor with format validation, ownership badges, suggestion engine with reason tags, mini analytics, auto-complete with preview, buildability percentage on deck list and detail, full analytics charts, and "Add missing to wantlist" integration.
  </what-built>
  <how-to-verify>
1. Navigate to http://localhost:3000/decks and verify:
   - Each deck card shows Draft/Complete badge
   - Each deck card shows buildability percentage (e.g., "You own 12/53 (23%)")

2. Click on a deck to view detail page:
   - Verify Draft/Complete badge in header
   - Switch to "Analytics" tab -- verify energy curve bar chart, domain pie chart, card type breakdown, rarity distribution render correctly
   - Verify buildability section shows ownership count and percentage
   - If missing cards exist, verify "Add X missing to wantlist" button works

3. Click "Edit" to open the deck card editor:
   - Verify zone tabs: Main | Runes | Champion | Sideboard with counts
   - Switch between zone tabs and verify card search auto-filters (Main hides Legends and Runes, Runes tab only shows Runes, Champion only shows Legends)
   - Verify ownership badges appear on search result cards
   - Add a card you don't own -- verify wantlist prompt toast appears
   - Fill a zone to capacity, try to add another card -- verify swap prompt appears
   - Verify validity status bar at top: shows red errors for incomplete deck, green "Valid" for valid deck
   - Switch to "Suggested" tab in search panel -- verify suggestion cards with reason tags
   - Toggle "Owned first" / "Best fit" in suggestions panel
   - Click a reason tag to see expanded detail
   - If empty slots exist, click "Auto-complete" -- verify preview of cards to be added appears before confirmation

4. Create a new deck via wizard (Legend path):
   - Verify "Build with my cards" and "Build best deck" options appear after selecting a legend
   - Try both options and verify different card selections

5. Save a deck and verify:
   - Status updates to Draft or Complete based on validation
   - Returning to deck list shows updated status badge
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/web/tsconfig.json` passes
- All 5 analytics charts render from deck card data
- Buildability shows on both deck list and detail pages
- "Add missing to wantlist" button functions correctly
- Draft/Complete badges appear on list and detail views
</verification>

<success_criteria>
Deck list and detail pages surface collection awareness (buildability %) and deck quality metrics (analytics charts, draft/complete status). Players can immediately see how close they are to completing any deck and one-tap add all missing cards to their wantlist.
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-smart-deck-builder/01.2-04-SUMMARY.md`
</output>
