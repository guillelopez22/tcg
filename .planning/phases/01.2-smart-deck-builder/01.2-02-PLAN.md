---
phase: 01.2-smart-deck-builder
plan: 02
type: execute
wave: 2
depends_on: ["01.2-01"]
files_modified:
  - apps/api/src/modules/deck/deck.service.ts
  - apps/api/src/modules/deck/deck.router.ts
  - apps/api/src/modules/deck/keyword-tag.service.ts
  - apps/api/src/modules/deck/keyword-tag.module.ts
  - apps/api/src/modules/deck/deck.module.ts
  - apps/api/src/trpc/trpc.module.ts
  - apps/api/__tests__/deck.service.spec.ts
  - apps/api/__tests__/deck-analytics.spec.ts
  - apps/api/__tests__/keyword-tag.service.spec.ts
autonomous: true
requirements:
  - DECK-01
  - DECK-02
  - DECK-04

must_haves:
  truths:
    - "setCards validates zone-specific limits: main=40, rune=12, champion=1, sideboard=0 or 8"
    - "setCards enforces 3-copy limit across main+sideboard combined"
    - "setCards enforces 1-copy limit for Signature card types"
    - "Deck status is computed from validation: 'complete' if all zone sizes correct, 'draft' otherwise"
    - "Suggestion endpoint returns ranked cards with reason tags"
    - "Buildability endpoint returns owned count, total, percentage, and missing card IDs"
    - "Keyword tagging cron runs daily and tags cards with synergy keywords"
    - "computeAnalytics pure function has unit test coverage for energy curve, domain distribution, and null guards"
    - "tagKeywords method has unit test coverage for keyword parsing, null descriptions, and log output"
  artifacts:
    - path: "apps/api/src/modules/deck/deck.service.ts"
      provides: "Zone-aware validateFormat (delegates to shared), suggest(), getBuildability(), updated setCards/create"
      exports: ["DeckService"]
    - path: "apps/api/src/modules/deck/deck.router.ts"
      provides: "suggest and buildability tRPC procedures"
    - path: "apps/api/src/modules/deck/keyword-tag.service.ts"
      provides: "Daily cron for keyword tagging on card descriptions"
    - path: "apps/api/__tests__/deck.service.spec.ts"
      provides: "Tests for zone validation, copy limits, buildability"
    - path: "apps/api/__tests__/deck-analytics.spec.ts"
      provides: "Tests for computeAnalytics pure function: energy curve, domain distribution, null guards"
    - path: "apps/api/__tests__/keyword-tag.service.spec.ts"
      provides: "Tests for tagKeywords: keyword regex parsing, null descriptions, log output"
  key_links:
    - from: "apps/api/src/modules/deck/deck.service.ts"
      to: "packages/shared/src/utils/validate-deck-format.ts"
      via: "imports validateDeckFormat for zone validation"
      pattern: "validateDeckFormat"
    - from: "apps/api/src/modules/deck/deck.service.ts"
      to: "packages/shared/src/constants/card.constants.ts"
      via: "imports MAX_COPIES_PER_CARD, SIGNATURE_TYPES, zone constants"
      pattern: "MAX_COPIES_PER_CARD|SIGNATURE_TYPES|DECK_ZONES"
    - from: "apps/api/src/modules/deck/deck.router.ts"
      to: "packages/shared/src/schemas/deck.schema.ts"
      via: "imports deckSuggestSchema, deckBuildabilitySchema"
      pattern: "deckSuggestSchema|deckBuildabilitySchema"
    - from: "apps/api/src/modules/deck/keyword-tag.service.ts"
      to: "packages/db/src/schema/cards.ts"
      via: "writes to cards.keywords column"
      pattern: "keywords"
---

<objective>
Service layer overhaul: zone-aware format validation, suggestion engine, buildability queries, keyword tagging cron, and test coverage for analytics and keyword tagging.

Purpose: Replace the permissive 60-card/12-copy validation with correct Riftbound rules (40+12+1, 3-copy, 0/8 sideboard), add the suggestion and buildability endpoints that the editor UI needs, set up keyword tagging for synergy-based suggestions, and establish test coverage for all pure functions (computeAnalytics, tagKeywords).

Output: Updated DeckService with zone validation, new tRPC procedures (suggest, buildability), keyword tagging cron service, and comprehensive tests.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-smart-deck-builder/01.2-CONTEXT.md
@.planning/phases/01.2-smart-deck-builder/01.2-RESEARCH.md
@.planning/phases/01.2-smart-deck-builder/01.2-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (to be created) -->
From packages/shared/src/constants/card.constants.ts (after Plan 01):
```typescript
export const DECK_ZONES = ['main', 'rune', 'champion', 'sideboard'] as const;
export type DeckZone = (typeof DECK_ZONES)[number];
export const MAX_COPIES_PER_CARD = 3;
export const MAX_SIGNATURE_COPIES = 1;
export const MAIN_DECK_SIZE = 40;
export const RUNE_DECK_SIZE = 12;
export const CHAMPION_COUNT = 1;
export const SIDEBOARD_SIZE = 8;
export const SIGNATURE_TYPES = ['Signature Unit', 'Signature Spell', 'Signature Gear'] as const;
export function getZoneForCardType(cardType: string | null): DeckZone;
```

From packages/shared/src/utils/validate-deck-format.ts (after Plan 01):
```typescript
export function validateDeckFormat(
  entries: Array<{cardId: string; quantity: number; zone: string}>,
  cardTypeMap: Map<string, string | null>
): string[];
```

From packages/shared/src/schemas/deck.schema.ts (after Plan 01):
```typescript
export const deckSuggestSchema = z.object({
  deckId: z.string().uuid(),
  mode: z.enum(['owned_first', 'best_fit']).default('best_fit'),
  zone: z.enum(DECK_ZONES).default('main'),
});
export const deckBuildabilitySchema = z.object({ deckId: z.string().uuid() });
export type DeckSuggestInput = z.infer<typeof deckSuggestSchema>;
export type DeckBuildabilityInput = z.infer<typeof deckBuildabilitySchema>;
```

From apps/api/src/modules/deck/deck.service.ts (current):
```typescript
export class DeckService {
  constructor(private readonly db: DbClient) {}
  // Current: MAX_DECK_CARDS = 60, MAX_COPIES_PER_CARD = 12
  // Methods: list, getById, create, update, delete, setCards, browse
  // Private: validateCardEntries, validateCardIdsExist
}
```

From apps/api/src/modules/deck-recommendations/deck-recommendations.service.ts:
```typescript
// Existing pattern: Redis injection via @Inject('REDIS_CLIENT')
// Uses ioredis for caching, same injection available in DeckService
```

From packages/db/src/schema/decks.ts (after Plan 01):
```typescript
// deckCards now has zone column
// decks now has status column
```

From packages/db/src/schema/cards.ts (after Plan 01):
```typescript
// cards now has keywords TEXT[] column
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zone-aware format validation + buildability in DeckService</name>
  <files>apps/api/src/modules/deck/deck.service.ts, apps/api/__tests__/deck.service.spec.ts</files>
  <behavior>
    - Test: valid 40+12+1 deck with no sideboard -> 0 errors, status 'complete'
    - Test: valid 40+12+1+8 sideboard -> 0 errors, status 'complete'
    - Test: main deck with 35 cards -> error "Main: 35/40"
    - Test: rune deck with 10 runes -> error "Runes: 10/12"
    - Test: no champion -> error "Need 1 champion"
    - Test: sideboard with 5 cards -> error "Sideboard: 5 (must be 0 or 8)"
    - Test: 4 copies of same card across main+sideboard -> error "Too many copies"
    - Test: 2 copies of Signature card -> error "Signature card limit: 1 copy"
    - Test: buildability returns correct owned count, total, pct, missingCardIds
  </behavior>
  <action>
1. In `deck.service.ts`:
   - Remove old `MAX_DECK_CARDS = 60` and `MAX_COPIES_PER_CARD = 12` constants
   - Import `validateDeckFormat` from `@la-grieta/shared` (the shared pure function from Plan 01)
   - Import `MAX_COPIES_PER_CARD, MAX_SIGNATURE_COPIES, MAIN_DECK_SIZE, RUNE_DECK_SIZE, CHAMPION_COUNT, SIDEBOARD_SIZE, SIGNATURE_TYPES` from `@la-grieta/shared`
   - Import `collections` from `@la-grieta/db` for buildability queries

2. Use `validateDeckFormat` from shared package in setCards/create methods instead of duplicating validation logic. The server calls the same pure function the client will use.

3. Compute `status` from validation: if `validateDeckFormat` returns empty array -> 'complete', else -> 'draft'. Save status on deck after setCards.

4. Update `setCards` method:
   - Accept zone-aware entries (each entry has cardId, quantity, zone)
   - Load card types for all cardIds to build cardTypeMap
   - Call validateDeckFormat -- errors do NOT block save (deck saved as 'draft')
   - Delete existing deck_cards and insert new ones WITH zone field
   - Update deck status based on validation result
   - Return updated deck with cards

5. Update `create` method similarly -- cards now include zone field, compute status on create.

6. Update `getById` to include zone in the cardRows select.

7. Add `getBuildability(userId: string, deckId: string)`:
   - Load deck cards (all zones) with their quantities
   - Load user's collection (grouped by cardId with count)
   - For each deck card entry: check if user owns >= quantity
   - Return `{ owned: number, total: number, pct: number, missingCardIds: string[] }`

8. Update `DeckCardWithCard` type to include `zone: string`.

9. Extend existing tests in `deck.service.spec.ts`:
   - Add test group for format validation covering all the behaviors listed above (note: these test the integration of DeckService with the shared validateDeckFormat)
   - Add test for buildability calculation
   - Update existing setCards/create tests to include zone in card entries
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && cd apps/api && pnpm vitest run __tests__/deck.service.spec.ts --reporter=dot</automated>
  </verify>
  <done>DeckService validates zone-specific limits via shared validateDeckFormat, computes deck status, returns buildability data. All new and existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suggestion engine + router wiring</name>
  <files>apps/api/src/modules/deck/deck.service.ts, apps/api/src/modules/deck/deck.router.ts, apps/api/src/modules/deck/deck.module.ts</files>
  <behavior>
    - Test: suggest() returns array of SuggestedCard objects with score and reasonTag fields
    - Test: suggest() with mode 'owned_first' boosts owned cards in ranking
    - Test: suggest() filters candidates by zone-appropriate card types
  </behavior>
  <action>
1. Add `suggest()` method to `DeckService`:
   - Input: `{ deckId, mode: 'owned_first' | 'best_fit', zone }` (from DeckSuggestInput)
   - Inject Redis client (same pattern as DeckRecommendationsService: `@Inject('REDIS_CLIENT')`)
   - Cache key: `deck:suggest:${deckId}:${mode}:${zone}`, TTL 10 minutes
   - Logic:
     a. Load current deck cards to understand what's already in the deck
     b. Determine what's needed: if zone is 'main', find cards that are not Legend or Rune type
     c. Load candidate cards from DB (filter by appropriate card types for zone)
     d. For each candidate, compute score:
        - Domain match: +3 if card domain matches deck's dominant domain
        - Curve balance: +2 if card fills a gap in the energy curve (energyCost bucket with fewest cards)
        - Co-occurrence: +2 if card appears in trending/public decks that share the same champion
        - Keyword synergy: +1 per matching keyword between card and existing deck cards (reads from cards.keywords column)
     e. If mode === 'owned_first': boost owned cards by +5
     f. Sort by score descending, take top 15
     g. Generate reason tag for each: pick highest-scoring signal ("Meta pick", "Curve filler", "Synergy: {keyword}", "Domain match", "Owned")
     h. Return `SuggestedCard[]` with `{ cardId, card: {...}, score, reasonTag, reasonDetail, owned: boolean }`
   - Define `SuggestedCard` type in deck.service.ts

2. Update `DeckService` constructor to accept Redis via `@Inject('REDIS_CLIENT')` -- follow existing pattern from DeckRecommendationsService.

3. Update `deck.module.ts` to provide REDIS_CLIENT to DeckService (same ioredis factory pattern as DeckRecommendationsModule).

4. Update `deck.router.ts`:
   - Import `deckSuggestSchema`, `deckBuildabilitySchema` from `@la-grieta/shared`
   - Add `suggest: proc.input(deckSuggestSchema).query(...)` calling `this.deckService.suggest(ctx.userId, input)`
   - Add `buildability: proc.input(deckBuildabilitySchema).query(...)` calling `this.deckService.getBuildability(ctx.userId, input.deckId)`
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && npx tsc --noEmit -p apps/api/tsconfig.json && cd apps/api && pnpm vitest run __tests__/deck.service.spec.ts --reporter=dot</automated>
  </verify>
  <done>Suggestion endpoint returns ranked SuggestedCard[] with reason tags, cached in Redis with 10-min TTL. Router exposes suggest and buildability as tRPC procedures. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Keyword tagging cron + analytics tests + keyword tests</name>
  <files>apps/api/src/modules/deck/keyword-tag.service.ts, apps/api/src/modules/deck/keyword-tag.module.ts, apps/api/src/trpc/trpc.module.ts, apps/api/__tests__/keyword-tag.service.spec.ts, apps/api/__tests__/deck-analytics.spec.ts</files>
  <behavior>
    - keyword-tag tests: tagKeywords parses "ACCELERATE" keyword from card description
    - keyword-tag tests: tagKeywords parses "discard" and "draw" action keywords from description text
    - keyword-tag tests: tagKeywords handles cards with null descriptions gracefully (no crash, no keywords)
    - keyword-tag tests: tagKeywords logs count of tagged cards on completion
    - deck-analytics tests: computeAnalytics groups energy costs into correct buckets (0-7, 8+)
    - deck-analytics tests: computeAnalytics aggregates domain distribution with null -> "Neutral"
    - deck-analytics tests: computeAnalytics handles cards with null energyCost (treated as 0)
    - deck-analytics tests: computeAnalytics handles empty card array -> empty distributions
  </behavior>
  <action>
1. Create `apps/api/src/modules/deck/keyword-tag.service.ts`:
   - Injectable service with `@Inject('DB_CLIENT') private readonly db: DbClient`
   - `@Cron('0 7 * * *')` decorator (daily at 07:00 UTC, after deck-sync at 06:00)
   - `tagKeywords()` method:
     - Load all cards with descriptions from DB
     - Parse keywords from card descriptions using regex matching:
       - ACCELERATE, ASSAULT, MULTIATTACK, BARRIER, VIGILANCE, DRAIN, OVERWHELM (uppercase keywords in text)
       - "discard" -> discard, "draw" -> draw, "heal"/"restore" -> heal, "+X might"/"give a unit" -> buff, "sacrifice"/"destroy" -> sacrifice
     - Store as TEXT[] in the `keywords` column on the cards table (column added by Plan 01 migration)
   - Log count of tagged cards on completion
   - Export the keyword parsing logic as a standalone pure function `parseKeywords(description: string | null): string[]` so it can be unit tested without DB

2. Create `apps/api/src/modules/deck/keyword-tag.module.ts`:
   - Standard NestJS module importing ScheduleModule.forRoot() (check if already imported in app module -- if so, skip forRoot)
   - Provide KeywordTagService with DB_CLIENT injection

3. Update `apps/api/src/trpc/trpc.module.ts` to import KeywordTagModule for registration.

4. Create `apps/api/__tests__/keyword-tag.service.spec.ts`:
   - Test `parseKeywords()` pure function directly (no DB mocking needed)
   - Test: description containing "ACCELERATE" -> keywords includes "accelerate"
   - Test: description containing "discard a card and draw 2" -> keywords includes "discard" and "draw"
   - Test: description containing "heal" or "restore" -> keywords includes "heal"
   - Test: null description -> returns empty array
   - Test: description with no matching keywords -> returns empty array
   - Test: verify count logging output (mock console.log or Logger)

5. Create `apps/api/__tests__/deck-analytics.spec.ts`:
   - Import or define the `computeAnalytics` pure function. NOTE: This function will be created in Plan 04 Task 2 as part of `deck-analytics.tsx`. For now, define the function signature and implement it as a shared utility OR test it as inline logic.
   - Preferred approach: Extract `computeAnalytics` as a pure function in `packages/shared/src/utils/compute-analytics.ts` so it can be tested here. If Plan 04 creates it in the component, move the pure logic to shared.
   - Test cases:
     - Cards with energyCost [1,2,2,3,5,8,10] -> energy curve groups: {1:1, 2:2, 3:1, 5:1, "8+":2}
     - Cards with domains ["Fire","Fire","Water",null] -> domain distribution: {Fire:2, Water:1, Neutral:1}
     - Cards with null energyCost -> treated as 0 in curve
     - Empty card array -> all distributions empty
     - Cards with quantities > 1 -> quantity multiplied in counts
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && cd apps/api && pnpm vitest run __tests__/keyword-tag.service.spec.ts __tests__/deck-analytics.spec.ts --reporter=dot</automated>
  </verify>
  <done>Keyword tagging cron service created with daily schedule, writing to cards.keywords column. keyword-tag.service.spec.ts covers keyword parsing, null handling, and logging. deck-analytics.spec.ts covers computeAnalytics with energy curve grouping, domain distribution, null guards, and empty arrays.</done>
</task>

</tasks>

<verification>
- `cd apps/api && pnpm vitest run __tests__/deck.service.spec.ts` -- all tests pass including new zone validation and buildability tests
- `cd apps/api && pnpm vitest run __tests__/keyword-tag.service.spec.ts` -- keyword parsing tests pass
- `cd apps/api && pnpm vitest run __tests__/deck-analytics.spec.ts` -- analytics pure function tests pass
- `npx tsc --noEmit -p apps/api/tsconfig.json` -- no type errors
- `deck.router.ts` exports `suggest` and `buildability` procedures
- `keyword-tag.service.ts` exists with @Cron decorator and parseKeywords export
- DeckService.suggest() returns SuggestedCard[] with reason tags
</verification>

<success_criteria>
The API layer fully supports zone-aware deck operations: format validation with correct Riftbound rules (via shared utility), deck status computation, card suggestions with scoring and reason tags, buildability percentage with missing card lists, daily keyword tagging, and comprehensive test coverage for all pure functions including computeAnalytics and parseKeywords.
</success_criteria>

<output>
After completion, create `.planning/phases/01.2-smart-deck-builder/01.2-02-SUMMARY.md`
</output>
