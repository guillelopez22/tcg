---
phase: 01-collection-tracker
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - apps/web/src/app/(dashboard)/collection/page.tsx
  - apps/web/src/app/(dashboard)/collection/collection-grid.tsx
  - apps/web/src/app/(dashboard)/collection/collection-tabs.tsx
  - apps/web/src/app/(dashboard)/collection/add-cards-modal.tsx
  - apps/web/src/app/(dashboard)/collection/[cardId]/page.tsx
  - apps/web/src/app/(dashboard)/collection/[cardId]/copy-list.tsx
  - apps/web/src/app/(dashboard)/collection/[cardId]/copy-edit-form.tsx
  - apps/web/src/app/(dashboard)/collection/wantlist-tab.tsx
  - apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx
  - apps/web/src/components/ui/photo-upload.tsx
  - apps/web/src/app/globals.css
autonomous: false
requirements:
  - COLL-01
  - COLL-03
  - COLL-04
  - COLL-05
  - COLL-06
  - COLL-07
  - COLL-08

must_haves:
  truths:
    - "User sees a card image grid with copy count badges on the Collection tab"
    - "User can tap a card to navigate to a detail page showing all copies with edit controls"
    - "User can open a floating + button to add cards via search multi-select modal"
    - "User can edit a copy's variant, condition, purchase price, notes, and photo"
    - "User can delete a copy via red 'Remove this copy' button with confirmation"
    - "User can filter collection by set, rarity, variant, condition, domain"
    - "User can sort collection by name, date added, price, set number"
    - "User can switch between Collection, Wantlist, Tradelist, Stats tabs"
    - "User can toggle a card on/off wantlist and tradelist from the card detail page"
    - "Wantlist and tradelist tabs show their respective lists with empty states"
    - "All UI strings use t() function from next-intl"
  artifacts:
    - path: "apps/web/src/app/(dashboard)/collection/page.tsx"
      provides: "Four-tab collection page layout"
      contains: "CollectionTabs"
    - path: "apps/web/src/app/(dashboard)/collection/collection-grid.tsx"
      provides: "Card image grid with copy count badges and filters"
      contains: "copyCount"
    - path: "apps/web/src/app/(dashboard)/collection/add-cards-modal.tsx"
      provides: "Bottom sheet modal with multi-select card search"
      contains: "fuse"
    - path: "apps/web/src/app/(dashboard)/collection/[cardId]/page.tsx"
      provides: "Card detail page with copy list and wantlist/tradelist toggles"
      contains: "getByCard"
    - path: "apps/web/src/app/(dashboard)/collection/[cardId]/copy-edit-form.tsx"
      provides: "Per-copy edit form with variant, condition, price, photo, notes"
      contains: "purchasePrice"
    - path: "apps/web/src/app/(dashboard)/collection/wantlist-tab.tsx"
      provides: "Wantlist display with card grid and empty state"
      contains: "wantlist"
    - path: "apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx"
      provides: "Tradelist display with card grid and empty state"
      contains: "tradelist"
  key_links:
    - from: "apps/web/src/app/(dashboard)/collection/collection-grid.tsx"
      to: "/api via tRPC"
      via: "trpc.collection.list.useQuery"
      pattern: "collection\\.list"
    - from: "apps/web/src/app/(dashboard)/collection/add-cards-modal.tsx"
      to: "/api via tRPC"
      via: "trpc.collection.addBulk.useMutation"
      pattern: "collection\\.addBulk"
    - from: "apps/web/src/app/(dashboard)/collection/[cardId]/page.tsx"
      to: "/api via tRPC"
      via: "trpc.collection.getByCard + trpc.wishlist.getForCard"
      pattern: "collection\\.getByCard|wishlist\\.getForCard"
    - from: "apps/web/src/app/(dashboard)/collection/[cardId]/copy-edit-form.tsx"
      to: "/api via tRPC"
      via: "trpc.collection.update + trpc.collection.getUploadUrl"
      pattern: "collection\\.update|collection\\.getUploadUrl"
---

<objective>
Build the full collection UI: four-tab layout (Collection | Wantlist | Tradelist | Stats), card image grid with copy count badges, add cards modal, card detail page with per-copy editing, wantlist/tradelist tabs, and photo upload.

Purpose: This is the primary interface users interact with to manage their collection. Every CRUD operation from Plan 02 gets a UI surface. All strings use next-intl t() from Plan 01.

Output: Fully interactive collection management UI with grid, detail, add, edit, delete, wantlist/tradelist toggle, filtering, and sorting.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-collection-tracker/01-CONTEXT.md
@.planning/phases/01-collection-tracker/01-RESEARCH.md
@.planning/phases/01-collection-tracker/01-01-SUMMARY.md
@.planning/phases/01-collection-tracker/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collection page with tabs, card grid, filters, and add modal</name>
  <files>
    apps/web/src/app/(dashboard)/collection/page.tsx,
    apps/web/src/app/(dashboard)/collection/collection-tabs.tsx,
    apps/web/src/app/(dashboard)/collection/collection-grid.tsx,
    apps/web/src/app/(dashboard)/collection/add-cards-modal.tsx,
    apps/web/src/app/(dashboard)/collection/wantlist-tab.tsx,
    apps/web/src/app/(dashboard)/collection/tradelist-tab.tsx,
    apps/web/src/app/globals.css
  </files>
  <action>
    1. Rework `apps/web/src/app/(dashboard)/collection/page.tsx`:
       - Client component with four tabs: Collection | Wantlist | Tradelist | Stats
       - Use `CollectionTabs` component for tab switching
       - Stats tab renders a placeholder div (built in Plan 05)
       - All strings via `useTranslations('collection')` and `useTranslations('nav')`

    2. Create `collection-tabs.tsx`:
       - Tab bar component with 4 tabs using `lg-*` design system classes
       - Active tab state managed via URL search params or React state
       - Mobile-first: horizontal scroll if tabs overflow on small screens

    3. Rework `collection-grid.tsx` (replaces old `collection-manager.tsx`):
       - Fetch cards via `trpc.collection.list.useQuery` with current filters
       - Display card image grid (3 cols on lg, 2 cols on md, 2 on mobile per user decision grid layout)
       - Copy count badge overlaid on each card thumbnail (top-right corner)
       - Each card is a link to `/collection/[cardId]` detail page
       - Filter bar: dropdowns for set, rarity, variant, condition, domain
       - Sort bar: dropdown for name/date_added/price/set_number with asc/desc toggle
       - Infinite scroll or "load more" button using cursor pagination
       - Empty state: illustration-style message + CTA to add cards
       - Floating + button (fixed bottom-right, above mobile nav) opens AddCardsModal

    4. Create `add-cards-modal.tsx`:
       - Bottom sheet / modal overlay triggered by the floating + button
       - Search input at top using `fuse.js` for client-side card fuzzy search (fuse.js already in web deps)
       - Card results displayed as a scrollable grid
       - Multi-select mode: tap a card to increment count (badge shows tap count), tap again to increment more
       - "Confirm" button at bottom shows count of cards selected
       - On confirm: call `trpc.collection.addBulk.useMutation` with all selected cards as Normal/NM (per user decision)
       - Success toast via `sonner` showing "{count} cards added"
       - Close modal after success

    5. Create `wantlist-tab.tsx`:
       - Fetch wantlist via `trpc.wishlist.list.useQuery({type: 'want'})`
       - Card grid similar to collection-grid but simpler (no copy count badges)
       - Each card links to `/collection/[cardId]` detail page
       - Show preferredVariant and maxPrice badges if set
       - Empty state: illustration + "Browse cards to add to your wantlist" CTA
       - All strings via `useTranslations('wantlist')`

    6. Create `tradelist-tab.tsx`:
       - Same pattern as wantlist-tab but with `type: 'trade'`
       - Show askingPrice badge if set
       - Empty state: "Add cards from your collection to your tradelist"
       - All strings via `useTranslations('tradelist')`

    7. Add any new `lg-*` component classes needed in `globals.css`:
       - `lg-tab-active`, `lg-tab-inactive` for tab styling
       - `lg-badge-count` for copy count overlay
       - `lg-fab` for floating action button
       - `lg-modal-backdrop`, `lg-modal-sheet` for bottom sheet

    IMPORTANT: Use `lg-*` design system classes from globals.css. Do NOT scatter raw Tailwind for common patterns. If a new pattern is needed, define it as an `lg-*` class first.
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && pnpm --filter @la-grieta/web build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Collection page has four functional tabs
    - Card grid displays cards with copy count badges from tRPC data
    - Filters (set, rarity, variant, condition, domain) and sorts (name, date, price, set#) work
    - Floating + button opens add-cards modal
    - Add-cards modal supports multi-select search and bulk add
    - Wantlist and tradelist tabs display their respective lists with empty states
    - All strings use t() function
    - Web builds successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Card detail page with copy list, edit form, and wishlist toggles</name>
  <files>
    apps/web/src/app/(dashboard)/collection/[cardId]/page.tsx,
    apps/web/src/app/(dashboard)/collection/[cardId]/copy-list.tsx,
    apps/web/src/app/(dashboard)/collection/[cardId]/copy-edit-form.tsx,
    apps/web/src/components/ui/photo-upload.tsx
  </files>
  <action>
    1. Create `apps/web/src/app/(dashboard)/collection/[cardId]/page.tsx`:
       - Client component, extract `cardId` from route params
       - Fetch card data via `trpc.card.getById` (or similar existing procedure)
       - Fetch all copies via `trpc.collection.getByCard.useQuery({cardId})`
       - Fetch wishlist status via `trpc.wishlist.getForCard.useQuery({cardId})`
       - Layout: large card art at top, card name + set + rarity info
       - Below art: wantlist star icon (filled if on wantlist) and tradelist star icon (filled if on tradelist)
         - Tapping toggles via `trpc.wishlist.toggle.useMutation`
         - Show toast on toggle success ("Added to wantlist" / "Removed from wantlist")
       - Below toggles: "Copies ({count})" section header with copy list
       - "Add another copy" button that calls `trpc.collection.add.useMutation` with Normal/NM defaults

    2. Create `copy-list.tsx`:
       - List of all copies the user owns for this card
       - Each copy row shows: variant badge, condition badge, purchase price (if set), photo thumbnail (if set), date added
       - Tap a copy row to expand inline edit form (CopyEditForm)
       - Only one copy expanded at a time (accordion pattern)

    3. Create `copy-edit-form.tsx`:
       - Inline form for editing a single copy
       - Fields: variant dropdown (Normal, Alt-Art, Overnumbered, Signature), condition dropdown (NM, LP, MP, HP, Damaged), purchase price input (numeric with $ prefix), notes textarea
       - Photo upload section using PhotoUpload component
       - "Save" button calls `trpc.collection.update.useMutation` with changed fields only
       - Red "Remove this copy" button at bottom
         - On tap: confirmation dialog ("Are you sure you want to remove this copy?")
         - On confirm: call `trpc.collection.remove.useMutation`, show success toast, remove from list
       - Variant dropdown: for now, show all 4 variants for all cards (variant validation per card deferred per RESEARCH.md open question — ship all enabled, enforce by convention). Add a comment noting this should be card-specific when data is available.
       - All strings via `useTranslations('collection')`

    4. Create `apps/web/src/components/ui/photo-upload.tsx`:
       - Reusable component for R2 photo uploads
       - Props: `purpose: string`, `currentPhotoUrl?: string`, `onUploadComplete: (url, key) => void`
       - UI: shows current photo if exists, "Upload Photo" button or drag area
       - Flow: call `trpc.collection.getUploadUrl.useMutation({contentType})` → PUT to presigned URL → call onUploadComplete
       - Show upload progress indicator
       - Max file size: 5MB, accept image/jpeg and image/png
       - Error handling: toast on failure, retry button

    IMPORTANT: The "Remove this copy" button must be RED per user decision. Confirmation dialog is REQUIRED — no auto-delete.
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && pnpm --filter @la-grieta/web build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Card detail page shows large art, card info, and all user copies
    - Wantlist and tradelist star toggle icons work from detail page
    - Copy list shows variant, condition, price, photo for each copy
    - Inline edit form allows changing variant, condition, price, notes, photo
    - Photo upload works via R2 presigned URL flow
    - Red "Remove this copy" button with confirmation dialog works
    - All strings use t() function
    - Web builds successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify collection UI end-to-end</name>
  <action>Human verification checkpoint — review the built features following the steps below.</action>
  <what-built>Complete collection management UI: four-tab layout, card grid with copy badges, add cards modal with multi-select, card detail page with per-copy editing, photo upload, wantlist/tradelist toggle, filtering, and sorting. All built on top of the per-copy API from Plan 02.</what-built>
  <how-to-verify>
    1. Start dev servers: `pnpm dev` (or `pnpm --filter @la-grieta/api dev` and `pnpm --filter @la-grieta/web dev`)
    2. Login with test@test.com / PassWord123
    3. Navigate to /collection — verify four tabs visible: Collection | Wantlist | Tradelist | Stats
    4. Tap the floating + button — verify add-cards modal opens with search
    5. Search for a card, tap it multiple times to increase count, confirm — verify cards added
    6. On the collection grid, verify copy count badges appear on cards
    7. Test filters: select a set, a rarity — verify grid updates
    8. Test sort: change to "Name A-Z" — verify order changes
    9. Tap a card — verify detail page shows large art, all copies, wantlist/tradelist stars
    10. Tap wantlist star — verify it fills and toast appears
    11. Tap a copy row — verify edit form expands with variant, condition, price, notes, photo
    12. Change variant to Alt-Art, save — verify it updates
    13. Tap "Remove this copy" — verify confirmation dialog, then deletion
    14. Switch to Wantlist tab — verify the card you starred appears
    15. Switch to Tradelist tab — verify empty state with CTA
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter @la-grieta/web build` succeeds
2. Collection grid renders with card images and copy count badges
3. Add cards modal supports multi-select search and bulk add
4. Card detail page shows all copies with inline editing
5. Wantlist and tradelist tabs functional with toggle from detail page
6. All strings use t() translation function
</verification>

<success_criteria>
- Four-tab collection page renders and switches tabs
- Card grid shows collection with copy count badges, filters, and sorting
- Add cards modal enables multi-select bulk add
- Card detail page shows large art, all copies with edit controls, wantlist/tradelist toggles
- Photo upload works end-to-end via R2
- Copy deletion has red button + confirmation dialog
- Wantlist and tradelist tabs show their lists with empty states
- All UI text goes through next-intl t()
</success_criteria>

<output>
After completion, create `.planning/phases/01-collection-tracker/01-03-SUMMARY.md`
</output>
