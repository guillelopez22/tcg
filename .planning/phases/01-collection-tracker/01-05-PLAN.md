---
phase: 01-collection-tracker
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - apps/api/src/modules/collection/collection.service.ts
  - apps/api/src/modules/deck-recommendations/deck-recommendations.module.ts
  - apps/api/src/modules/deck-recommendations/deck-recommendations.service.ts
  - apps/api/src/modules/deck-recommendations/deck-recommendations.router.ts
  - apps/api/__tests__/deck-recommendations.service.spec.ts
  - apps/api/src/app.module.ts
  - apps/api/src/trpc/trpc.router.ts
  - apps/web/src/app/(dashboard)/collection/stats-tab.tsx
  - apps/web/src/app/(dashboard)/collection/stats-charts.tsx
  - apps/web/src/app/(dashboard)/collection/deck-recommendations.tsx
  - apps/web/messages/en.json
  - apps/web/messages/es.json
  - tools/seed/tournament-decks.json
  - tools/seed/seed-tournament-decks.ts
autonomous: false
requirements:
  - COLL-09
  - COLL-10
  - PLAT-01

must_haves:
  truths:
    - "User can see total unique cards, total copies, and total market value on the Stats tab"
    - "User can see per-set completion progress bars (owned vs total in set)"
    - "User can see value breakdown by set using market prices (normal vs foil variant pricing)"
    - "User can see rarity distribution (Common/Uncommon/Rare/Epic breakdown)"
    - "User can see deck recommendations with ownership %, missing cards with prices, and synergy reasoning"
    - "User can one-tap 'Add all missing to wantlist' from a deck recommendation"
    - "Stats market value uses TCGplayer market price for normal, foil market price for alt_art/overnumbered/signature"
    - "Spanish translations are complete for all collection-related UI strings"
    - "Language toggle switches between EN and ES"
  artifacts:
    - path: "apps/api/src/modules/deck-recommendations/deck-recommendations.service.ts"
      provides: "Synergy engine for deck recommendations"
      exports: ["getRecommendations"]
    - path: "apps/web/src/app/(dashboard)/collection/stats-tab.tsx"
      provides: "Stats tab with totals, set completion, value breakdown, rarity distribution"
      contains: "totalValue"
    - path: "apps/web/src/app/(dashboard)/collection/stats-charts.tsx"
      provides: "Recharts visualizations for stats"
      contains: "recharts"
    - path: "apps/web/src/app/(dashboard)/collection/deck-recommendations.tsx"
      provides: "Deck recommendation cards with ownership %, missing cards, synergy reasoning"
      contains: "ownership"
    - path: "apps/web/messages/es.json"
      provides: "Complete Spanish translations"
      contains: "Coleccion"
    - path: "tools/seed/tournament-decks.json"
      provides: "Seed data for 5-10 tournament decks"
      contains: "champion"
  key_links:
    - from: "apps/web/src/app/(dashboard)/collection/stats-tab.tsx"
      to: "apps/api/src/modules/collection/collection.service.ts"
      via: "trpc.collection.stats.useQuery"
      pattern: "collection\\.stats"
    - from: "apps/web/src/app/(dashboard)/collection/deck-recommendations.tsx"
      to: "apps/api/src/modules/deck-recommendations/deck-recommendations.router.ts"
      via: "trpc.deckRecommendations.getRecommendations.useQuery"
      pattern: "deckRecommendations\\.getRecommendations"
    - from: "apps/api/src/modules/collection/collection.service.ts"
      to: "packages/db/src/schema/card-prices.ts"
      via: "Drizzle join for market value"
      pattern: "cardPrices"
---

<objective>
Build the stats tab with market value calculations, set completion, rarity distribution, and deck recommendations with synergy engine. Complete the Spanish translation pass and add language toggle.

Purpose: Stats and deck recommendations are the payoff for digitizing a collection. Users see the value of what they own, their completion progress, and intelligent suggestions for decks they can almost build. Spanish translations complete the i18n requirement.

Output: Fully functional Stats tab with charts and deck recommendations, complete ES translations, language toggle.
</objective>

<execution_context>
@C:/Users/guill/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/guill/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-collection-tracker/01-CONTEXT.md
@.planning/phases/01-collection-tracker/01-RESEARCH.md
@.planning/phases/01-collection-tracker/01-01-SUMMARY.md
@.planning/phases/01-collection-tracker/01-02-SUMMARY.md
@.planning/phases/01-collection-tracker/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend stats service with market values + create deck recommendations module</name>
  <files>
    apps/api/src/modules/collection/collection.service.ts,
    apps/api/src/modules/deck-recommendations/deck-recommendations.module.ts,
    apps/api/src/modules/deck-recommendations/deck-recommendations.service.ts,
    apps/api/src/modules/deck-recommendations/deck-recommendations.router.ts,
    apps/api/__tests__/deck-recommendations.service.spec.ts,
    apps/api/src/app.module.ts,
    apps/api/src/trpc/trpc.router.ts,
    tools/seed/tournament-decks.json,
    tools/seed/seed-tournament-decks.ts
  </files>
  <behavior>
    - stats(userId) returns totalUniqueCards (distinct cardIds), totalCopies (row count), totalMarketValue (summed from card_prices with variant mapping)
    - stats(userId) returns setCompletion: array of {setId, setName, owned, total, percentage}
    - stats(userId) returns valueBySet: array of {setId, setName, totalValue} using marketPrice for normal variant and foilMarketPrice for alt_art/overnumbered/signature
    - stats(userId) returns rarityDistribution: {common, uncommon, rare, epic} counts
    - stats(userId) uses new cache keys (e.g., cache:stats_v2:userId) to avoid stale cache from old shape
    - getRecommendations(userId) returns top 5 decks sorted by ownership percentage descending
    - Each recommendation includes: deckName, champion, ownershipPct, ownedCards[], missingCards[] (with market prices), synergyReasoning (text explaining why this deck matches)
    - getRecommendations(userId) returns empty array gracefully when no decks exist in DB
    - Synergy scoring considers: shared domain/legend affinity between user's cards and deck cards, overlap of card effects/keywords
  </behavior>
  <action>
    1. Create `tools/seed/tournament-decks.json`:
       - Research and create 5-10 representative Riftbound tournament decks
       - Each deck: name, champion cardId, description, array of card entries (cardId + quantity)
       - Mark as `isPublic: true` and add a `source: 'tournament'` or `isTournament: true` flag
       - If actual tournament lists are unavailable, create reasonable sample decks using the seeded card data (query the cards table or card JSON to find champions and build thematic decks)

    2. Create `tools/seed/seed-tournament-decks.ts`:
       - Script that reads tournament-decks.json and inserts into `decks` and `deck_cards` tables
       - Uses a system user ID or creates a "La Grieta" system user for seeded decks
       - Idempotent: check if deck with same name exists before inserting
       - Run with: `pnpm tsx tools/seed/seed-tournament-decks.ts`

    3. Write tests in `apps/api/__tests__/deck-recommendations.service.spec.ts`:
       - Test getRecommendations returns sorted by ownership %
       - Test missing cards include market prices
       - Test empty deck DB returns empty array
       - Test synergy reasoning is a non-empty string for each recommendation

    4. Extend `collection.service.ts` stats() method:
       - Add totalMarketValue: join collections with card_prices, sum using variant-to-price mapping:
         - normal -> cardPrices.marketPrice
         - alt_art, overnumbered, signature -> cardPrices.foilMarketPrice
       - Put this mapping in a shared constant (e.g., `VARIANT_PRICE_COLUMN` in shared/constants) — do NOT hardcode in multiple places per RESEARCH.md anti-pattern warning
       - Add valueBySet: group by setId, sum market values
       - Add rarityDistribution: group by card rarity, count
       - Use new cache keys (e.g., `cache:user_stats_v2:${userId}`) with 5-min TTL for user-specific stats — do NOT cache with old keys per RESEARCH.md Pitfall 2

    5. Create `deck-recommendations.service.ts`:
       - `@Injectable()` class, inject db + redis
       - `getRecommendations(userId)`:
         a. Get user's collection card IDs
         b. Query public decks (from seeded tournament decks + community decks) with their deck_cards
         c. For each deck: calculate ownership % (cards user owns / total cards in deck)
         d. Synergy scoring: bonus points for shared domain between user's cards and deck cards, shared legend affinity keywords in descriptions, matching rune colors
         e. Generate synergy reasoning text: "This deck shares X domain cards with your collection and uses Y cards you already own"
         f. For missing cards: look up market prices from card_prices table
         g. Sort by composite score (ownership % * 0.7 + synergy score * 0.3) — weights per Claude's discretion
         h. Return top 5

    6. Create `deck-recommendations.router.ts`:
       - `getRecommendations`: protected procedure, no input required (uses ctx.userId)
       - Returns array of recommendation objects

    7. Create `deck-recommendations.module.ts`, register in app.module.ts and trpc.router.ts.

    8. Run seed script: `pnpm tsx tools/seed/seed-tournament-decks.ts`
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && pnpm --filter @la-grieta/api test -- --reporter=verbose deck-recommendations 2>&1 | tail -20</automated>
  </verify>
  <done>
    - stats() returns totalUniqueCards, totalCopies, totalMarketValue, setCompletion, valueBySet, rarityDistribution
    - Market value correctly maps variant to price column
    - Deck recommendations returns top 5 decks sorted by ownership %
    - Each recommendation has synergy reasoning text explaining the match
    - Missing cards include market prices
    - Tournament deck seed data exists and can be inserted
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Stats tab UI with charts, deck recommendations, Spanish translations, and language toggle</name>
  <files>
    apps/web/src/app/(dashboard)/collection/stats-tab.tsx,
    apps/web/src/app/(dashboard)/collection/stats-charts.tsx,
    apps/web/src/app/(dashboard)/collection/deck-recommendations.tsx,
    apps/web/messages/en.json,
    apps/web/messages/es.json,
    apps/web/src/components/language-toggle.tsx,
    apps/web/package.json
  </files>
  <action>
    1. Install recharts: `pnpm add recharts --filter @la-grieta/web`

    2. Create `stats-tab.tsx`:
       - Fetch stats via `trpc.collection.stats.useQuery()`
       - Layout (mobile-first, 375px base):
         - Top section: 3 stat cards in a row — Total Unique Cards, Total Copies, Total Market Value (formatted as currency)
         - Set Completion section: per-set progress bars with "X/Y cards (Z%)" labels
         - Value Breakdown section: bar chart by set using recharts
         - Rarity Distribution section: pie/donut chart using recharts
         - Deck Recommendations section: renders DeckRecommendations component
       - All strings via `useTranslations('stats')`
       - Loading skeletons while data fetches

    3. Create `stats-charts.tsx`:
       - `SetValueChart`: Recharts ResponsiveContainer with BarChart showing value per set
         - X-axis: set names, Y-axis: total value in $
         - Tooltip showing exact value on hover
         - Use design system colors
       - `RarityChart`: Recharts PieChart/donut showing rarity distribution
         - Segments: Common (gray), Uncommon (green), Rare (blue), Epic (purple)
         - Labels showing count per rarity
       - Both charts must be responsive (ResponsiveContainer) and work on mobile

    4. Create `deck-recommendations.tsx`:
       - Fetch via `trpc.deckRecommendations.getRecommendations.useQuery()`
       - Card layout for each recommendation:
         - Champion card art thumbnail
         - Deck name, "X% of cards owned" progress bar
         - Synergy reasoning text (italicized, explaining WHY this deck is recommended)
         - "Missing cards" expandable section showing each missing card with name + market price
         - "Add all missing to wantlist" button
           - Calls `trpc.wishlist.toggle.useMutation` for each missing card with type='want'
           - Shows toast: "X cards added to wantlist"
           - Button disables after use, changes to "Added to wantlist"
       - Empty state: "Build your collection to get deck recommendations"
       - All strings via `useTranslations('stats')`

    5. Update `apps/web/messages/en.json`:
       - Add any missing keys discovered during implementation
       - Ensure all new stats and deck recommendation strings are present

    6. Complete `apps/web/messages/es.json` with proper Spanish translations:
       - Translate ALL keys from en.json to Spanish
       - Use Latin American Spanish appropriate for Honduras
       - Key translations: Collection=Coleccion, Wantlist=Lista de deseos, Tradelist=Lista de intercambio, Stats=Estadisticas, Scanner=Escaner, Deck Recommendations=Mazos recomendados, etc.
       - DO NOT use placeholder/English values — translate everything

    7. Create `apps/web/src/components/language-toggle.tsx`:
       - Simple toggle button/dropdown showing current language (EN/ES)
       - On click: sets `locale` cookie to the other language, reloads page (or uses next-intl's locale switching)
       - Place in profile dropdown or navigation header
       - Style with `lg-*` design system classes

    8. Wire language toggle into the dashboard navigation (SmartNav or DashboardNav component).
  </action>
  <verify>
    <automated>cd C:/Users/guill/OneDrive/Desktop/Cooking/TCG && pnpm --filter @la-grieta/web build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - Stats tab shows total unique cards, total copies, total market value
    - Set completion progress bars render for each set
    - Value breakdown bar chart renders with recharts
    - Rarity distribution chart renders with recharts
    - Deck recommendations show with ownership %, synergy reasoning, missing cards + prices
    - "Add all missing to wantlist" button works
    - es.json has complete Spanish translations
    - Language toggle switches between EN and ES
    - Web builds successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify stats, recommendations, and i18n end-to-end</name>
  <action>Human verification checkpoint — verify stats, recommendations, and i18n following the steps below.</action>
  <what-built>Stats tab with market value calculations, set completion bars, value breakdown charts, rarity distribution, deck recommendations with synergy reasoning, complete Spanish translations, and language toggle.</what-built>
  <how-to-verify>
    1. Navigate to /collection and click the Stats tab
    2. Verify stat cards: total unique cards, total copies, total market value
    3. Verify set completion progress bars (e.g., "Origins: 15/200 cards (7.5%)")
    4. Verify value breakdown bar chart renders by set
    5. Verify rarity distribution pie/donut chart renders
    6. Scroll to deck recommendations — verify at least one recommendation appears (after seeding)
    7. Verify recommendation shows: champion art, deck name, ownership %, synergy reasoning text
    8. Expand missing cards — verify card names and market prices shown
    9. Tap "Add all missing to wantlist" — verify toast and button state change
    10. Switch to Wantlist tab — verify missing cards appear
    11. Find the language toggle (nav or profile) — switch to Spanish
    12. Verify ALL visible text changed to Spanish (Collection=Coleccion, Stats=Estadisticas, etc.)
    13. Switch back to English — verify text returns to English
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm --filter @la-grieta/api test -- deck-recommendations` passes
2. `pnpm --filter @la-grieta/api test -- collection.service` passes (stats tests)
3. `pnpm --filter @la-grieta/web build` succeeds
4. Stats tab renders with all sections populated
5. Deck recommendations show synergy reasoning
6. Spanish translations complete in es.json
7. Language toggle functional
</verification>

<success_criteria>
- Stats tab shows total unique/copies/value, set completion bars, value breakdown chart, rarity chart
- Market value correctly uses normal vs foil pricing based on variant
- Deck recommendations show top 5 decks with ownership %, synergy reasoning, missing cards + prices
- "Add all missing to wantlist" works end-to-end
- All UI strings translated to Spanish in es.json
- Language toggle switches between EN and ES and all text updates
- Tournament deck seed data exists for recommendations to use
</success_criteria>

<output>
After completion, create `.planning/phases/01-collection-tracker/01-05-SUMMARY.md`
</output>
