# ================================
# La Grieta TCG Platform
# Docker Environment Configuration
# ================================

# ================================
# Database Configuration
# ================================
POSTGRES_DB=lagrieta
POSTGRES_USER=lagrieta_user
POSTGRES_PASSWORD=ChangeMeToSecurePassword123
POSTGRES_PORT=5432

# ================================
# API Configuration
# ================================
API_PORT=3000

# JWT Configuration
JWT_SECRET=ChangeMeToRandomString32CharsMin
JWT_EXPIRES_IN=7d

# Riot API (Optional - uses mock data if not provided)
RIOT_API_KEY=

# ================================
# Scanner Configuration
# ================================
# Google Cloud Vision API Key (Optional - uses mock mode if not provided)
GOOGLE_CLOUD_VISION_API_KEY=

# Scanner mode: true for mock OCR, false for live Google Cloud Vision API
SCANNER_MOCK_MODE=true

# Maximum image size in megabytes
SCANNER_MAX_IMAGE_SIZE_MB=10

# ================================
# Web Frontend Configuration
# ================================
WEB_PORT=80

# ================================
# Production Deployment Notes
# ================================
# 1. ALWAYS change POSTGRES_PASSWORD to a strong password
# 2. ALWAYS change JWT_SECRET to a long random string (minimum 32 characters)
# 3. Use environment-specific values for different environments (dev, staging, prod)
# 4. Never commit the actual .env.docker file to version control
# 5. Consider using Docker secrets for sensitive data in production
# 6. For HTTPS, use a reverse proxy like Traefik or nginx-proxy with Let's Encrypt
