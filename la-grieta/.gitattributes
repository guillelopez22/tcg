# Set default behavior
* text=auto

# Shell scripts must have Unix line endings for Docker
*.sh text eol=lf

# Batch files should have Windows line endings
*.bat text eol=crlf
*.cmd text eol=crlf

# Other files that should have Unix line endings
Dockerfile* text eol=lf
docker-compose*.yml text eol=lf
*.conf text eol=lf
