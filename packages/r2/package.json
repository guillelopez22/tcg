{
  "name": "@la-grieta/r2",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare R2 client and presigned URL utilities for La Grieta",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc --project tsconfig.json",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@la-grieta/tsconfig": "workspace:*",
    "@types/node": "^20.0.0"
  }
}
